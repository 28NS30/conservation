{
 "confirmed": [
  {
   "id": "receipt-404",
   "area": "reporting",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "day",
   "evidence": "SUCCESS SCREEN ALWAYS LINKS: apps/web/components/report/ReportForm.tsx:227-245 — the `phase === \"done\"` branch renders `<a href={`/reports/${result.id}`}>` (line 238) unconditionally; the only thing that varies is the sentence above it (line 235: `awaitingIdentification ? t(\"identifying\") : t(\"published\")`). The form stores only `id` and `awaitingIdentification` from the response (lines 169-172) even though the API also returns `status` (apps/web/app/api/reports/route.ts:158-166).\n\nWHICH SUBMISSIONS ARE PENDING: route.ts:105-108 — `awaitingId = classifiable && !identified`; `status = awaitingId || flaggedReason ? \"pending\" : \"published\"`. `requiresClassification` is `CATEGORIES[category].classifiable && photoCount > 0` (packages/shared/src/index.ts:285-290). `screenSubmission` (apps/web/lib/abuse.ts:71-91) flags: coordinates outside Taiwan (78), classifiable category with zero photos (80-82), links/handles in notes (84-88). So every classifiable report without a reporter-named species, and every flagged report, is inserted as `pending`.\n\nPAGE READS ONLY reports_public AND 404s: apps/web/app/[locale]/(site)/reports/[id]/page.tsx:54-72 — query is `from reports_public rp ... where rp.id = ${id}` via `asPublic`, then `if (!row) notFound();` (line 72). The owner lookup on `reports` (lines 106-111) runs only after that and is used for `canEdit`, not visibility. The view is `where status = 'published' and location_precision <> 'suppressed'` (supabase/migrations/0010_capture_accuracy.sql:33-54; same predicate in 0001_init.sql:237-244). The 404 page (apps/web/app/[locale]/not-found.tsx:8-10) says \"The link may be out of date, or this record does not exist.\" (messages/en.json:282-283; zh-TW.json:282-283). curl: GET /reports/00000000-0000-4000-8000-000000000000 → 404, /en/... → 404; a published seed id (69937252-...) → 200 on both paths, so the code path is live.\n\n/me LINKS PENDING ROWS TO THE SAME PAGE: apps/web/app/[locale]/(site)/me/page.tsx:89-101 reads `reports` (all statuses, by design per the comment at 45-49); lines 206-212 wrap every row, including `pending` and `rejected`, in `<Link href={`/reports/${r.id}`}>`. No status guard.\n\nTWO THINGS THE REVIEW UNDERSTATES: (1) The window is not \"a minute or two\". en.json:119 promises that, but the classifier is a Vercel cron at `0 3 * * *` (vercel.json:3-6; classify route header at apps/web/app/api/jobs/classify/route.ts:5-8), and there is no on-submit trigger (grep for jobs/classify, after(, waitUntil in app/lib/components finds nothing). Publication happens only in that job (classify/route.ts:194-202 and 232-242) or by a moderator (apps/web/app/[locale]/(site)/admin/actions.ts:26-35). So an identification-pending report 404s for up to ~24 h; a flagged one 404s until a human acts. (2) For a flagged submission, `awaitingIdentification` is false while `status` is `pending`, so the success screen affirmatively says \"Published to the map.\" (en.json:120) and then the link 404s.\n\nSIDE FINDINGS: ReportForm.tsx:238 is a plain `<a>`, not the locale-aware `Link` from apps/web/i18n/navigation.ts:4-10, so under /en it drops the prefix (curl shows the unprefixed path renders `<html lang=\"zh-TW\">`). apps/web/components/report/QueueBanner.tsx:155-166 (\"view sent\" receipts after an offline flush) links to the same page and has the same 404 for pending reports.\n\nWhat I inferred rather than ran: I did not create a pending report locally (write to the DB is out of scope), so the 404 for a pending id is established from the view predicate plus the observed 404 on an absent id, not from a pending row.",
   "fix": "Minimal (hours): (a) ReportForm.tsx:169-172 keep `status` from the response; at 237-242 render the link only when `status === \"published\"`, otherwise show a status sentence (new keys in messages/en.json and zh-TW.json under `report`, e.g. `heldForReview`, plus a pointer to /me for signed-in reporters); fix the \"usually a minute or two\" copy at en.json:119 / zh-TW.json:119 to match the daily cron, or add an on-submit trigger of /api/jobs/classify. (b) Replace the `<a>` at ReportForm.tsx:238 with `Link` from `@/i18n/navigation`. (c) me/page.tsx:211-212 and QueueBanner.tsx:159-160: only wrap in a Link when status is published; render pending/rejected rows as plain blocks with the existing Journey/Status markers.\n\nBetter (day): in reports/[id]/page.tsx, after the reports_public miss at line 70 and before `notFound()` at 72, read `reports` on the privileged connection for `id = ${id} and reporter_id = ${userId}` (userId from `currentRole()`, currently fetched at 106); if found, render an owner-only \"held\" view (category, observed date, Journey state, own photos; no coordinates/map) instead of 404. Then /me and the success screen can keep linking for signed-in reporters; anonymous reporters still need the conditional link from (a) because they have no session to prove ownership."
  },
  {
   "id": "status-copy",
   "area": "reporting",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "Both halves of the claim hold, and the review's stated mechanism matches the code exactly.\n\n1. Success copy is chosen from awaitingIdentification alone.\n- apps/web/components/report/ReportForm.tsx:69-72 stores only `{ id, awaitingIdentification }` from the response; :170-173 sets it from `data.awaitingIdentification` (the `status` field the API also returns is discarded); :235 renders `result.awaitingIdentification ? t(\"identifying\") : t(\"published\")`.\n- apps/web/app/api/reports/route.ts:71-77 computes `flaggedReason` via `screenSubmission`; :105-108 `awaitingId = classifiable && !identified; status = awaitingId || flaggedReason ? \"pending\" : \"published\"`; :158-166 returns `status` (correct) and `awaitingIdentification: awaitingId` (independent of flaggedReason).\n- apps/web/lib/abuse.ts:78-88 returns exactly the three reasons the review names: \"coordinates outside Taiwan\", \"no photo on a category that expects one\", \"links or handles in notes\".\n- All four live categories are classifiable (packages/shared/src/index.ts:15-18), and requiresClassification is `classifiable && photoCount > 0` (:285-290). So: no photo => classifiable=false => awaitingId=false, flaggedReason set => status 'pending' but copy \"Published to the map.\" Same for a photo+named-species report whose coordinates fall outside Taiwan or whose notes contain a link. The form even contradicts itself: before submit it shows report.noPhotoWarning \"without one the report goes to manual review\" (ReportForm.tsx:247-248, 395-397), then after submit says published.\n- Worse, the \"View this report\" link (ReportForm.tsx:237-241 -> /reports/{id}) reads through `reports_public` and calls notFound() for any unpublished row (apps/web/app/[locale]/(site)/reports/[id]/page.tsx:54-72), so every pending reporter, including the \"identifying\" case, gets a 404 immediately after being told it is published / will appear shortly.\n\n2. \"usually a minute or two\" is not backed by anything in the code.\n- en.json report.identifying = 'Being identified now; it will appear on the map shortly (usually a minute or two).' (apps/web/messages/en.json, report.* dump); zh-TW.json:119 has the same promise (\"通常一兩分鐘\").\n- Classification runs only via POST /api/jobs/classify, documented as cron-driven (apps/web/app/api/jobs/classify/route.ts:5-8). Nothing triggers it on submit: grep for `jobs/classify`, `after(`, `waitUntil` in app/components/lib finds no caller; no pg_cron/pg_net in migrations.\n- The only cron declaration is the repo-root vercel.json (\"0 3 * * *\", i.e. daily, not minutes). apps/web/vercel.json declares no crons. docs/launch-checklist.md:52-55 and the project memory note (vercel-root-directory.md, confirmed via `vercel project inspect` on 2026-09-17) state the Vercel Root Directory is apps/web so the root file is ignored and the crons have never run in production. I could not verify production from here; that part is taken from docs/memory. Even if the cron were moved, the schedule is daily. docs/launch-checklist.md:235 still says \"every minute\", which is stale.\n- Even with a running worker, \"will appear on the map\" is not guaranteed: after MAX_ATTEMPTS the report is set back to 'pending' with flagged_reason 'classification unavailable' (classify/route.ts:258-268) and needs a moderator (admin/actions.ts:29).\n\nHTTP checks (no DB writes): POST /api/reports with `{}` -> 400 validation_failed (route is live and is this code); GET /en/report -> 200 and the served HTML contains \"usually a minute or two\", \"Published to the map\" and \"manual review\" (the warning and the contradicting success copy are both in the same page bundle).",
   "fix": "1. apps/web/components/report/ReportForm.tsx: keep `status` from the response (it is already returned at route.ts:161; also correct for the duplicate-nonce path at :133-136). Store `{ id, status, awaitingIdentification }` (:69-72, :170-173). At :235 branch three ways: status === 'published' -> t(\"published\"); awaitingIdentification -> t(\"identifying\"); else -> new t(\"heldForReview\"). At :237-241 render the \"View this report\" link only when status === 'published' (the detail page 404s otherwise), or replace it with plain text for pending reports.\n2. apps/web/messages/en.json and apps/web/messages/zh-TW.json (report.*): add report.heldForReview (\"Held for a quick review before it appears on the map.\" / zh equivalent); rewrite report.identifying to drop the time promise (\"We will identify the species before it appears on the map; that can take a while.\" / remove \"通常一兩分鐘\"). Optionally make report.published truthful too: it is only reached when the trigger-derived precision is set, so \"Published to the map.\" is fine as is.\n3. Optional, apps/web/app/api/reports/route.ts:158-166: add `heldForReview: Boolean(flaggedReason)` if you want the form not to infer from status; not required.\n4. Separate owner decision, not part of this fix: moving the crons from root vercel.json into apps/web/vercel.json (memory says this switches on a paid GPU endpoint). Also docs/launch-checklist.md:235 says \"every minute\" but the file says daily; update when touching this.\nNo tests reference awaitingIdentification (grep hits only the route and the form), so the change is contained."
  },
  {
   "id": "location-default",
   "area": "reporting",
   "verdict": "partly",
   "severity": "low",
   "effort": "hours",
   "evidence": "Mechanism confirmed verbatim. apps/web/components/report/LocationPicker.tsx:74-76 creates a red draggable Marker at `value ? [value.lng, value.lat] : TAIWAN_CENTER` and adds it to the map on mount; TAIWAN_CENTER = [120.98, 23.7] (packages/shared/src/index.ts:127). Lines 67-72 fitBounds the whole island, so the default pin is visible on first paint. LocationPicker.tsx:126-133 renders the lat/lng readout only when `value` is set, so the only in-picker cue that nothing is chosen is the missing readout and the wider zoom. ReportForm.tsx:56 starts `location` as null; :119 and :553 block submit on `!location`; :253-254 makes needLocation the first blocker. ReportForm.tsx:458-459 always shows t(\"tapToAdjust\") = \"Tap the map to adjust\" (apps/web/messages/en.json:102, zh-TW.json:102 \"點地圖可調整位置\") under the map even when nothing is chosen, which presupposes a location exists. The repo's own comment at ReportForm.tsx:538-541 (commit eb3e5eb, 2026-08-17, \"Say why the submit button is disabled\") says exactly what the review says: \"The picker drops a pin on Taiwan's centre before anything is chosen, so the form looks complete while `location` is still null\". Default pin position is never copied into state, so no wrong coordinate can be submitted. curl http://localhost:3000/en/report -> HTTP 200; SSR HTML contains `<p class=\"-mb-1 text-center text-xs text-ink-500\">Tap the map to mark where you saw it</p><button type=\"button\" disabled=\"\" ...>Submit report</button>` and 0 coordinate readouts, confirming the disabled button is labelled with the reason on first load — but that label is at the bottom of the form, below the Details section (ReportForm.tsx:468-523), not beside the map. No test files reference LocationPicker or needLocation.",
   "correction": "The picker half of the claim is fully true: a marker is drawn at Taiwan's centre before any location exists and there is no explicit unselected state on the map. The consequence is overstated: since commit eb3e5eb the form does say why submission is blocked (\"Tap the map to mark where you saw it\" directly above the disabled button, SSR-verified), so it is friction, not a dead end. The review also misses the stronger supporting detail: the helper line under the map (ReportForm.tsx:458-459) always says \"Tap the map to adjust\", which actively implies a location is already set. The remedy the review asks for (explicit unselected state) has not been implemented; the blocker text was the workaround.",
   "fix": "1) apps/web/components/report/LocationPicker.tsx:74-76: create the Marker but do not `.addTo(h.map)` while `value` is null; call `addTo` (idempotent in MapLibre) in the click handler at :84-87 and in the sync effect at :102-111 the first time a value arrives. Optionally render a small overlay inside the container (:117-125) when `!value` (e.g. \"Tap to mark the location\") so the empty map has an explicit unselected state. 2) apps/web/components/report/ReportForm.tsx:458-459: make the helper text conditional — `location ? t(\"tapToAdjust\") : t(\"needLocation\")` (or a new key such as \"tapToMark\" added to apps/web/messages/en.json and zh-TW.json next to :102) — so the line beside the map, not just the one above the button, says a location is still required. 3) Update the now-stale comment at ReportForm.tsx:538-541 to reflect that the picker no longer drops a default pin. 4) Add a component test asserting no marker is added and needLocation text is shown when `value` is null (none exists today)."
  },
  {
   "id": "raw-errors",
   "area": "reporting",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "day",
   "evidence": "RAW CODES REACH THE USER (true).\n- apps/web/components/report/ReportForm.tsx:165-167 `throw new Error(data.error ?? \\`submission failed (${res.status})\\`)`; :209 `setError((e as Error).message)`; :525-529 renders `{error}` verbatim. `data.error` is the server's snake_case code.\n- Codes emitted by apps/web/app/api/reports/route.ts: bad_json (:28), validation_failed (:34), rate_limited (:52), challenge_failed (:56), photo_missing (:63), photo_too_large (:64), photo_bad_type (:66), taxon_not_found (:84), insert_failed (:169). apps/web/app/api/uploads/sign/route.ts: rate_limited (:23), bad_request (:31), bad_count (:35), sign_failed (:48).\n- Live check: `curl -X POST localhost:3000/api/reports --data 'not json'` -> 400 `{\"error\":\"bad_json\"}`; `--data '{}'` -> 400 `{\"error\":\"validation_failed\",\"issues\":[...]}`; `/api/uploads/sign --data '{\"count\":0}'` -> 400 `{\"error\":\"bad_count\"}`; `--data 'not json'` -> 400 `{\"error\":\"bad_request\"}`.\n- No translation table for these codes exists: apps/web/messages/en.json `report` namespace (:~100-145) has only needLocation/photoFailed/needChallenge etc.; the `errors` namespace (en.json:277-285, zh-TW same) is error-page copy (title/body/retry/backHome/notFound*), and grep finds no consumer mapping API codes anywhere in apps/web.\n- ReportForm.tsx:109 `${t(\"photoFailed\")}: ${(e as Error).message}` appends a raw browser/library message (apps/web/lib/image.ts:52 \"canvas 2d context unavailable\", :59 \"image encoding failed\", or a DOMException from createImageBitmap at :42).\n- ReportForm.tsx:203 `${t(\"queueFailed\")}: ...` is WORSE than the review says: `t` is bound to the `report` namespace (:42) but `queueFailed` lives under `offline` (en.json:273, zh-TW:~273). apps/web/i18n/request.ts:5-15 sets no getMessageFallback, so use-intl's default (node_modules/use-intl/dist/esm/development/initializeConfig-CUsOI8u2.js:5-7,14-16, joinPath(namespace,key)) renders the literal \"report.queueFailed: <raw IndexedDB message>\".\n- Same leak on the offline path: apps/web/lib/offline/flush.ts:139 stores `data.error` (raw code) in lastError, :70/:81/:130/:143 store English literals; apps/web/components/report/QueueBanner.tsx:225-227 renders `i.lastError` verbatim.\n\nPHOTO ERRORS LAND IN THE GENERAL SLOT (true).\n- One error state only: ReportForm.tsx:68. addFiles' catch (:109) writes into it. It is rendered once at :525-529, after the Details section (:468-523) and directly above Turnstile (:533) and the submit button (:547). The Photos section is :333-400; its help line at :391-393 is where an inline error would go, and nothing renders there. Server photo rejections (photo_missing/photo_too_large/photo_bad_type) land in the same general slot as raw codes. Geolocate failure (:415) also reuses the hint string t(\"tapToAdjust\") as an error in that slot.\n\nRELATED DEFECT THE REVIEW MISSED: ReportForm.tsx:138-142 awaits `storage.uploadToSignedUrl` but never inspects the result; storage-js returns `{data, error}` and does not throw (node_modules/@supabase/storage-js/dist/index.d.mts:946-954). A failed upload is silent until /api/reports answers photo_missing. flush.ts:80-81 does check it, so the form is inconsistent with the queue path.\n\nNo test pins any of this: apps/web/test/report-form.test.mjs covers category doors only; offline-challenge.test.mjs only asserts the 403 retry branch in flush.ts.\n\nInferred, not observed: I did not render the form in a browser (rules forbid headless browsers); placement is read from JSX order, and the \"report.queueFailed\" rendering is derived from the library default plus the absent config.",
   "correction": "The claim is right in substance; one example is mischaracterised. \"upload signing failed (403)\" is not a raw service message: it is a client-composed English literal at ReportForm.tsx:131-132 that discards the response body (which would carry rate_limited/bad_count/bad_request/sign_failed). The sign route never emits 403 (it emits 429/400/500 at uploads/sign/route.ts:23,31,35,48); a 403 there could only come from something in front of the app. It is still an untranslated string with a bare HTTP status, so the point stands. The review also understates the problem: the \"translated\" prefix on the queue-failure message (:203) is itself a broken key that renders as \"report.queueFailed\".",
   "fix": "1. apps/web/messages/en.json and zh-TW.json: add a `report.apiErrors` (or top-level `apiErrors`) namespace with a human sentence per code emitted by api/reports/route.ts and api/uploads/sign/route.ts (bad_json, validation_failed, rate_limited, challenge_failed, photo_missing, photo_too_large, photo_bad_type, taxon_not_found, insert_failed, bad_request, bad_count, sign_failed) plus `unknown` taking {status}.\n2. apps/web/components/report/ReportForm.tsx submit(): at :131-132 read the sign response body and carry its `error` code instead of composing \"upload signing failed (N)\"; at :165-167 throw a typed error {code, status} rather than Error(data.error); at :209 map code -> t(`apiErrors.${code}`) with t.has() fallback to apiErrors.unknown. Keep the raw code in console.error only.\n3. Add a second state `photoError` rendered inside the Photos section after :393 (before the heldForReview note), with aria-describedby on the file input at :371-386. Route addFiles failures (:109, drop the appended e.message from the visible text), photo_* server codes, and upload failures to it. At :138-142 check `error` from uploadToSignedUrl as flush.ts:80-81 does.\n4. :203 use `tOffline(\"queueFailed\")` (already bound at :44) instead of `t(\"queueFailed\")`.\n5. :415 give geolocate failure its own key and render it by the location control rather than reusing tapToAdjust in the general slot.\n6. apps/web/lib/offline/flush.ts:70,81,130,139,143: store a code (not English text) in lastError; apps/web/components/report/QueueBanner.tsx:225-227 translate it at render with the same table.\n7. Add a source-level test in apps/web/test/ asserting ReportForm never calls setError with a bare `.message` and that every code in the two route files has a key in both catalogs."
  },
  {
   "id": "injured-guidance",
   "area": "reporting",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "What the code does:\n\n- apps/web/components/report/ReportForm.tsx:297-321 — when the chosen group has more than one category, it renders the heading t(\"conditionLabel\") and one pill per category whose label is t(`condition.${k}`) and whose onClick is only setCategory(k). There is no block conditioned on category === \"injured\" anywhere in the file; the word \"injured\" appears only in comments (lines 49, 264-266).\n- apps/web/messages/en.json report.conditionLabel = \"Was it alive?\", report.condition.roadkill = \"Dead\", report.condition.injured = \"Alive, but hurt\". zh-TW.json: \"牠還活著嗎？\" / \"已死亡\" / \"還活著，但受傷\". No sibling hint key exists, and a grep for hotline/rescue/1999/救援/急救/動保 across apps/web, packages, supabase and docs found no rescue or referral text anywhere in either locale.\n- curl http://localhost:3000/en/report?category=injured → 200; the injured pill renders aria-pressed=\"true\" and the text immediately following it is \"Species | optional | I'm not sure what it was …\" — the form goes straight from the sub-choice into the species picker. curl http://localhost:3000/report?category=injured → 200, same structure in zh-TW.\n- Post-submit \"done\" screen, ReportForm.tsx:222-240: shows report.thanks (\"Thank you for your report!\"), report.received (\"Your report was received.\"), then report.identifying (\"Being identified now; it will appear on the map shortly…\") or report.published (\"Published to the map.\"), and a \"View this report\" link. Nothing varies by category.\n- Server side, apps/web/app/api/reports/route.ts:106-146: the insert sets status to pending/published and queues a classification_jobs row; there is no branch on input.category. apps/web/lib/abuse.ts:71-81 screenSubmission only checks classifiable+no-photo. supabase/migrations/0001_init.sql:137 constrains reports.status to ('pending','published','rejected') — there is no responded/dispatched state. A grep for resend/nodemailer/sendgrid/smtp/notif/twilio under apps/web/app, lib, components and supabase/migrations found no notification pipeline; supabase/functions does not exist.\n- The project's own copy confirms nobody responds: home.whoBody (en.json / zh-TW.json) = \"An independent volunteer project — not a government agency…\".\n- The code's own rationale for keeping injured as a separate stored category is that it \"implies someone should respond\" (packages/shared/src/index.ts:36-38; ReportForm.tsx:264-266; docs/plan-finish.md:86-88). The data model records that implication; the product neither acts on it nor tells the reporter that it will not.\n\nOn the review's stated reason: whether a reporter would \"assume a rescue has been dispatched\" is a perception claim the code cannot prove, but nothing on the page contradicts it — the door copy is \"An animal dead on the road, or alive and hurt\", the completion screen says \"Your report was received\" with no disclaimer, and the only \"what next\" text (identifying/published) is about the record's publication, not the animal. The reason is sound. One nuance: the done screen does say what happens next to the *record*; the claim is accurate in that it says nothing about what happens to the *animal* or whether anyone will act.\n\nNote on exposure: the form has never been used (page.tsx comment \"nobody has filed a report here yet\"; all records are imported), so zero user reports is not evidence the gap is harmless.",
   "fix": "Client-only change, no schema or API work:\n\n1. apps/web/messages/en.json and zh-TW.json, under \"report\": add e.g. \"condition.injuredHint\" stating that this records the sighting and no one is dispatched, and pointing to the right channel (the county/city Animal Protection Office, or Taiwan's 1999 citizen hotline — the owner should confirm the exact wording/agency before shipping; do not invent numbers). Add a matching \"injuredDone\" string for the completion screen.\n\n2. apps/web/components/report/ReportForm.tsx, directly after the sub-choice pills (after line ~319, inside the `REPORT_GROUPS[group].categories.length > 1` block): render `{category === \"injured\" && <p className=\"mt-2 text-[11px] text-amber-700\">{t(\"condition.injuredHint\")}</p>}` — same pattern already used for noPhotoWarning at lines 371-375. `category` is already in scope.\n\n3. ReportForm.tsx done screen (lines 222-240): when `category === \"injured\"`, add a line with t(\"injuredDone\") so the \"received\" message cannot be read as \"help is on the way\".\n\n4. apps/web/test/report-form.test.mjs (already covers the sub-choice and the ?category=injured deep link at lines 21-124): add assertions that the hint text is present when injured is selected / on the deep link, and absent for Dead.\n\nOptional: the homepage door copy home.doorRoadkill could add a short \"(we record it; we cannot send help)\" but the form is where it must be."
  },
  {
   "id": "form-emphasis",
   "area": "reporting",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "Both halves of the claim are true in the code and in the HTML the dev server actually serves.\n\nUNIFORM WEIGHT. Every section heading uses the same class string, `text-sm font-medium text-ink-700`: Type (apps/web/components/report/ReportForm.tsx:270), Photos (:334), Location (:405), and Species (apps/web/components/report/SpeciesPicker.tsx:105 and :134). Every `<label>` for the remaining fields is `mb-1 block text-sm font-medium text-ink-700`: Observed (:473), Notes (:490), Email (:508). Sections are stacked with one uniform gap, `space-y-6` (:260). The only field with a smaller heading is the condition sub-choice, `text-xs font-medium text-ink-600` (:300), which is intentionally a sub-question. Required-ness is not expressed in the hierarchy: location is the only hard requirement (:119, :253, :553) and photos are strongly recommended for classifiable categories (:247-248, :395-399), yet their headings are styled identically to the optional ones. The one differentiator is a text qualifier, `<span class=\"font-normal text-ink-500\">(optional)</span>`, on Species, Notes and Email (:493, :511; SpeciesPicker.tsx:134-137). Confirmed by curl: `GET /en/report` and `GET /report` both 200; extracted h2s \"Type\", \"Species optional\", \"Photos (0/4)\", \"Location\" all carry `text-sm font-medium text-ink-700`, and the three `<label>`s carry `mb-1 block text-sm font-medium text-ink-700` (same in zh-TW: 通報類型 / 物種 選填 / 照片 (0/4) / 位置 / 發現時間 / 備註 (選填) / 聯絡信箱 (選填)).\n\nBARE \"+\". The photo picker is a `<label>` rendered as an 80x80 dashed box (`grid h-20 w-20 ... border-dashed ... text-2xl`) whose only text node is the literal `+` (:369-370). Inside it, the `<input type=\"file\" accept=\"image/*\" capture=\"environment\" multiple>` is `sr-only` (:371-386) and has no `aria-label` or `title`; the only aria-label in the file is on the remove-photo button (:357). Because the wrapping label's text is \"+\", the input's accessible name is also \"+\". Rendered HTML confirms it verbatim: `<label class=\"grid h-20 w-20 ... text-2xl ...\">+<input type=\"file\" accept=\"image/*\" capture=\"environment\" multiple=\"\" class=\"sr-only\"/></label>`. The string \"Take or add photos\" (or any equivalent) does not exist anywhere: 0 hits in both served pages, and a repo-wide grep for take-or-add / add photo / 新增照片 / 拍照 / 上傳照片 across apps/ and packages/ returns only unrelated copy (messages/zh-TW.json:413 \"拍照通報\", a how-it-works step; :440 about-page body). Context the review under-states: the box is not floating in a void, it sits 8px under the \"Photos (0/4)\" heading (:334-339) and above a help line (:391-393, \"Photos are downscaled and stripped of EXIF...\"). But the affordance itself carries no verb.\n\nTwo things the review did not say. (1) Accessibility: the file input's accessible name is \"+\", so a screen reader gets no better hint than a sighted user does. (2) `capture=\"environment\"` (:374) — this is inference about browser behaviour, not something in the code — makes phone browsers open the rear camera directly rather than a chooser, so the control is effectively \"take a photo\" on mobile and \"choose a file\" on desktop; the review's proposed wording \"Take or add photos\" would over-promise on phones unless `capture` is dropped or a second, capture-less input is added.",
   "fix": "1. Label the photo control (apps/web/components/report/ReportForm.tsx:369-387). Inside the existing `<label>`, replace the lone `+` with the icon plus a short visible caption, e.g. `<span aria-hidden>+</span><span className=\"text-[11px] font-normal\">{t(\"addPhoto\")}</span>` and change the box from `text-2xl` to a vertical grid. This fixes the accessible name at the same time (the input's name becomes the label text); alternatively add `aria-label={t(\"addPhoto\")}` on the `<input>` at :371 if the visible text is kept minimal. Add `report.addPhoto` to apps/web/messages/en.json and apps/web/messages/zh-TW.json. Choose the verb honestly: keep `capture=\"environment\"` (:374) and say \"Take a photo\" / \"拍照\", or drop `capture` (or render two inputs, one with and one without) if picking from the library must work on phones, then \"Take or add photos\" is accurate.\n\n2. Express required-ness in the hierarchy. Mirror the existing `(optional)` span pattern (:493, :511) with a `(required)` / `(必填)` qualifier on Location (:405) and, when `CATEGORIES[category].classifiable` (:247-248), a `(recommended)` on Photos (:334-339); add `report.required` / `report.recommended` keys to both message files. If a stronger visual step is wanted, demote the optional Notes and Email labels (:490, :508) to the sub-heading scale already used for the condition sub-choice, `text-xs font-medium text-ink-600` (:300), so the two required/recommended sections read one notch above them.\n\nBoth changes are local to ReportForm.tsx plus two message keys per locale; no schema, API, or shared-package changes."
  },
  {
   "id": "map-mobile-nav",
   "area": "navigation",
   "verdict": "partly",
   "severity": "medium",
   "effort": "hours",
   "evidence": "TRUE — map header hides the nav on phones. apps/web/components/site/SiteHeader.tsx:97-99 renders the only <nav> with `hidden items-center gap-5 ${app ? \"sm:flex\" : \"md:flex\"}`; apps/web/app/[locale]/map/page.tsx:106-113 passes variant=\"app\". curl http://localhost:3000/map (HTTP 200): the header is `<header class=\"z-20 flex shrink-0 ... bg-paper-100/95 ...\">` and the page contains exactly one nav, `<nav class=\"hidden items-center gap-5 sm:flex\">`. No --breakpoint overrides exist in apps/web/app/globals.css (@theme at line 22) and there is no tailwind.config, so sm=640px: below that the nav is display:none.\n\nTRUE — no second phone row on the map. SiteHeader.tsx:130 wraps the second row in `{!app && (...)}`; the comment at lines 124-129 says this is deliberate (\"The map's own header stays single-row: there, vertical space is the instrument.\"). curl /species by contrast shows `<nav class=\"hidden items-center gap-5 md:flex\">` plus the row wrapper `<div class=\"flex items-center justify-between gap-4 border-t px-4 py-2 sm:px-5 md:hidden ...\">` containing `<nav class=\"flex items-center gap-5\">`. /species and /stats are under the (site) route group (apps/web/app/[locale]/(site)/layout.tsx:18 uses variant=\"page\"), so they get that row.\n\nFALSE — \"except via the footer\". The map renders SiteFooter variant=\"app\" (map/page.tsx:129-132). That variant (apps/web/components/site/SiteFooter.tsx:60-77) emits only the `legal` list built at lines 38-49: /about, /team (if published), /attribution, /privacy, a mailto and a LanguageSwitcher. Species and Statistics exist only in the site-variant footer's \"explore\" nav (SiteFooter.tsx:89-107), which /map never renders. curl /map: the whole document has exactly one href=\"/species\" and one href=\"/stats\", and both are streamed into the hidden nav's placeholders (`<div hidden id=\"S:9\"><a ... href=\"/species\">物種</a></div>`, `S:a` for /stats). Other internal links on /map: \"/\", /about x2, /attribution, /privacy, /report, /map, and one /reports link that is a `sr-only focus:not-sr-only` skip link (\"以列表檢視\"). So on a phone the only routes from /map to Species/Statistics are indirect: wordmark → home → home's second row, or tap a map point → tap the species name in ReportPanel (apps/web/components/map/ReportPanel.tsx:138-144 links to /species/{taxonId}) → that page's second row. Also no mobile menu exists anywhere (the only aria-expanded is MapFilters.tsx:128, the filter-panel toggle). Nuance: between 640-767px the map DOES show the inline nav while other pages show the second row; the dead end is strictly <640px.\n\nTRUE — current page never indicated. SiteHeader.tsx:100-108 and 137-145 apply one unconditional className to every link and set no aria-current; the component is a server component with no pathname access. Repo-wide grep for aria-current/usePathname finds only LanguageSwitcher.tsx:37 (aria-current=\"true\" on the locale button, not the page) and reports/(list)/page.tsx:185,198 (filter tabs). curl /species: the /species link carries the identical class `text-xs transition text-ink-600 hover:text-ink-900` as /stats in both header navs (2 each) and `transition hover:text-ink-900` in the footer (1 each); `aria-current=\"page\"` count is 0. curl /map likewise renders the /map link with the same class as its siblings.\n\nNo test covers this: grep of apps/web/test and apps/web/e2e for SiteHeader / nav rows finds nothing; the viewport loop in apps/web/e2e/pages.spec.mjs:87-100 only checks the home badge/doors, never /map navigation.",
   "correction": "The reachability problem is real but worse than stated: there is no footer route to Species or Statistics from the map. The map's footer is the compact `variant=\"app\"` strip carrying only About/Team/Attribution/Privacy/Contact/language. From /map on a phone (<640px) the only ways to Species or Statistics are wordmark → home → home's phone nav row, or tapping a map point and then the species name (which goes to a single species page, not the index). The omission is also deliberate (SiteHeader.tsx:124-129 comment), not an oversight — the author traded phone nav for map height. Everything else in the claim holds, including the missing current-page indication on every page.",
   "fix": "1. Phone nav on the map (apps/web/components/site/SiteHeader.tsx): remove the `!app &&` guard at line 130 and make the row's hide-class match the inline nav's show-point — `app ? \"sm:hidden\" : \"md:hidden\"` at line 132 — so the row appears only where the inline nav is hidden (<640px on the map, <768px elsewhere) and costs one ~33px line of map height only on phones. Update the comment at lines 124-129 accordingly. If the author wants to keep the map strictly single-row, the alternative is to add /species and /stats (and /map for the other pages) to the app-variant footer's link list in apps/web/components/site/SiteFooter.tsx:60-77, accepting primary nav in 10px footer type; a menu button would need client JS and a focus trap, which the existing comment explicitly avoids.\n\n2. Current-page indication: add a small client component (e.g. apps/web/components/site/NavLink.tsx) that uses `usePathname` from \"@/i18n/navigation\" (already exported at apps/web/i18n/navigation.ts:9 and used in LanguageSwitcher.tsx:26; it returns the locale-stripped path) and renders `<Link aria-current={active ? \"page\" : undefined} className={active ? \"text-ink-900 font-medium\" : ...}>` where `active = pathname === href || pathname.startsWith(href + \"/\")`. Use it in both nav loops (SiteHeader.tsx:100-108 and 137-145), passing the overlay/page colour classes as props; optionally also in the footer explore/about navs.\n\n3. Test: add an e2e case (apps/web/e2e/pages.spec.mjs or map.spec.mjs) that loads /map at 390x844 and asserts a visible `a[href=\"/species\"]` and `a[href=\"/stats\"]`, and that loads /species and asserts exactly one `a[aria-current=\"page\"][href=\"/species\"]` in the header."
  },
  {
   "id": "list-toggle",
   "area": "navigation",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "Map → list. apps/web/components/map/HeatmapView.tsx:969-985 is the only place `list.viewAsList` is rendered in the app (grep over apps/web/app, components, messages: one usage, plus the two message strings at messages/en.json:353 and messages/zh-TW.json:353). It is a next-intl `<Link>` whose className is `sr-only focus:not-sr-only focus:absolute ...` (line 981), so it is clipped from view until it receives keyboard focus; the comment at 956-968 calls it a skip link on purpose and explains it is placed before the canvas so a screen-reader/keyboard user can bypass the WebGL region. It DOES carry the active filter: `query: Object.fromEntries(new URLSearchParams(filterToQuery(filter)))` (971-976). Served HTML confirms this: `curl http://localhost:3000/en/map?group=roadkill` returns exactly one anchor pointing at /reports: `<a class=\"sr-only focus:not-sr-only ...\" href=\"/en/reports?group=roadkill\">View as list</a>`. No other chrome on the map offers the route: MapFilters.tsx, MapModeToggle.tsx, MapColourToggle.tsx and MapHints.tsx contain no Link/href to /reports (grep, exit 0 with only a comment hit in MapHints.tsx:17); the legend/toggle box at HeatmapView.tsx:1013-1120 holds only mode and colour toggles; the header nav array at components/site/SiteHeader.tsx:43-49 is /map, /species, /stats, /about (+/me) with no /reports entry; the footer's Explore list (components/site/SiteFooter.tsx:88-94) does contain `{ href: \"/reports\", label: t(\"list.title\") }`, but the /map page renders `<SiteFooter variant=\"app\">` (app/[locale]/map/page.tsx:129-132) and the curl shows that variant emits no /reports anchor at all. So a mouse or touch user on /map has no visible route to the list.\n\nList → map. app/[locale]/(site)/reports/(list)/page.tsx:171-341 renders category chips (175-215), the table and pagination; it computes `activeFilter` (158-163) and threads it through chips and paging via `withFilter` (164-169), but never emits a link to /map (grep for \"/map\" in that file: none). `curl http://localhost:3000/en/reports?group=roadkill` shows three anchors to `/en/map`, all with header/footer classes and all plain `/en/map` — none carry `group=roadkill`. So the reverse switch does not exist in any form, visible or hidden, and the shared-chrome links drop the filter.\n\nWhat the code does vs what I infer: the sr-only/focus classes and the single-anchor curl result are observed; that the link is invisible to a pointer user is inferred from Tailwind's `sr-only` (1px clipped box) — I did not render it in a browser per the task rules. One wording nit: it is a keyboard-focus skip link (`focus:not-sr-only`), so a sighted keyboard user who tabs into the map region will see it briefly; it is not literally screen-reader-only. That does not change the claim's substance.",
   "fix": "1) HeatmapView.tsx: add a visible List control that reuses the href already built at lines 971-976 — e.g. a small segmented \"Map | List\" (or a single \"List\" pill) placed inside the top chrome row next to MapFilters (lines 1000-1009) or at the top of the legend box (1024-1030), with the `!presentation` guard. Keep the existing sr-only link at 969-985 as-is (its job is tab-order bypass of the canvas; a visible control rendered after the container div would sit behind the canvas and zoom buttons in tab order, which is exactly what the comment at 956-961 says was wrong before). If you prefer one element, move the visible control before the container `<div ref={container}>` at 986 so it stays the first tab stop, and drop the sr-only classes.\n2) reports/(list)/page.tsx: add the counterpart. `activeFilter` is already computed at 158-163; render `<Link href={`/map${activeFilter ? `?${activeFilter}` : \"\"}`}>` in the chip nav (175-215) or beside PageHeader (173), marked aria-current where appropriate, so the list→map direction keeps group/taxonId/from/to.\n3) messages/en.json and messages/zh-TW.json: add `list.viewOnMap` (e.g. \"View on map\" / \"在地圖上檢視\") next to `viewAsList` at line 353 of each.\n4) Optional, separate: SiteHeader.tsx:43-49 has no /reports entry and SiteFooter's app variant hides the Explore list; adding a plain (filter-less) \"Report list\" nav item would also cure the dead end but does not replace the filter-carrying switch."
  },
  {
   "id": "species-search-drops-filter",
   "area": "navigation",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "WHAT THE CODE DOES\n\napps/web/components/species/SpeciesSearch.tsx:29 builds the new URL from scratch and hands it to router.replace:\n\n    router.replace(q ? `${pathname}?q=${encodeURIComponent(q)}` : pathname);\n\nThe component imports only useRouter and usePathname from @/i18n/navigation (line 5); it never reads the current search params (no useSearchParams, no prop for the filter). So whatever `filter=` was in the address bar is discarded on every debounced keystroke, and when the box is emptied it replaces with the bare pathname, which drops the filter too.\n\nThe page does depend on that param. apps/web/app/[locale]/(site)/species/(directory)/page.tsx:35-39 declares and reads `filter` from searchParams; lines 45-49 validate it against FILTERS and fall back to \"recorded\" when it is absent; line 51-55 pass it to listSpecies. So after the search box rewrites the URL, the server re-renders with filter=\"recorded\" regardless of what the user had picked.\n\nThe asymmetry is one-directional: the filter chips DO preserve the search (page.tsx:70 `query: { ...(q ? { q } : {}), filter: f }`), which shows the site intends q and filter to combine. Only the search box breaks the pairing.\n\nHTTP CONFIRMATION (curl against localhost:3000, zh-TW unprefixed paths; \"results\" = <li> count minus the 9 non-result <li> present on every page):\n- /species                              -> 200, active chip = filter=recorded, 80 results\n- /species?filter=invasive              -> 200, active chip = filter=invasive, 80 results\n- /species?q=%E9%BE%9C                  -> 200, active chip = filter=recorded, 12 results   (this is the URL the box produces after typing 龜 while on the invasive chip)\n- /species?q=%E9%BE%9C&filter=invasive  -> 200, active chip = filter=invasive, 2 results    (what the user expected to see)\nRendered active-chip anchors, from the saved HTML: `<a href=\"/species?q=%E9%BE%9C&filter=recorded\">有紀錄</a>` vs `<a href=\"/species?q=%E9%BE%9C&filter=invasive\">入侵種</a>`.\n\nSo the drop is user-visible: the chip highlight snaps back to 有紀錄/\"recorded\" and the result set changes from 2 to 12 items.\n\nREVIEW'S STATED REASON: correct. The mechanism really is a hand-built replacement URL that contains only q. (Note it is not a push-vs-replace problem; using router.push would drop the filter just the same. The defect is that the current params are never read.)\n\nThis is code reading plus server-rendered HTML; I did not drive a browser, so the \"snaps back after 250 ms\" timing is inferred from the setTimeout at line 27-30, not observed.\n\nHistory (read-only git log): SpeciesSearch.tsx last touched in fb879b5; the `filter?: string` searchParam appeared in page.tsx in 60f6c1c. The filter was layered on after the search box was written, which is consistent with the box never having been taught about it.",
   "fix": "Single-file change in apps/web/components/species/SpeciesSearch.tsx: carry the existing params forward instead of rebuilding the URL from only q.\n\nOption A (read the URL):\n  import { useSearchParams } from \"next/navigation\";\n  ...\n  const searchParams = useSearchParams();\n  ...\n  const params = new URLSearchParams(searchParams.toString());\n  if (q) params.set(\"q\", q); else params.delete(\"q\");\n  const qs = params.toString();\n  router.replace(qs ? `${pathname}?${qs}` : pathname);\nand add searchParams to the effect deps on line 32. The (directory)/loading.tsx already provides the Suspense boundary useSearchParams wants, and the page is already dynamic because it awaits searchParams, so no static-render regression.\n\nOption B (pass it down, avoids useSearchParams entirely): the page already computes the validated `filter` at page.tsx:45-49, so render `<SpeciesSearch initialQuery={q ?? \"\"} filter={filter} />` at page.tsx:62 and have the component write `{ pathname: \"/species\", query: { filter, ...(q ? { q } : {}) } }` via next-intl's router.replace (which accepts the same href object the chips use at page.tsx:68-71). Slightly cleaner since it reuses the page's own validation; the URL would then always carry filter=recorded explicitly, which is harmless.\n\nEither way, verify by typing while on ?filter=invasive and confirming the 入侵種 chip stays highlighted and the URL keeps filter=invasive; also clear the box and confirm the filter survives (line 29's bare-pathname branch is the second place it is dropped)."
  },
  {
   "id": "lang-drops-query",
   "area": "navigation",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "The claim and its stated reason are both correct: the switcher navigates with the bare pathname and never reads the query string.\n\nWHAT THE CODE DOES\n- apps/web/components/LanguageSwitcher.tsx:26-27, 38 — `const pathname = usePathname()` (from `@/i18n/navigation`) and `onClick={() => router.replace(pathname, { locale: l })}`. There is no `useSearchParams`, no `window.location.search`, and no `query` object anywhere in the file.\n- apps/web/i18n/navigation.ts:9-10 — these hooks are next-intl 4.13.5's `createNavigation(routing)`.\n- node_modules/next-intl/dist/esm/development/navigation/react-client/useBasePathname.js:16-32 — `usePathname` wraps Next's `usePathname()` (path only, no search) and strips the locale prefix.\n- node_modules/next-intl/dist/esm/development/navigation/react-client/createNavigation.js:35-57 — `replace(href, {locale})` runs `getPathname({href, locale, forcePrefix})` and calls Next's `router.replace(pathname)` with the result.\n- node_modules/next-intl/dist/esm/development/navigation/shared/createSharedNavigationFns.js:69-76 — a string `href` is used verbatim; a query string is only serialized when `href` is an object with `query`. So `/en/map?lng=…&lat=…&z=…` → click 中文 → `/zh-TW/map` (then middleware 307 to `/map`).\n- Because it is `replace`, not `push`, the pre-switch URL with the query is also overwritten in history, so Back does not recover it (inference from replace semantics).\n\nWHAT LIVES IN THE QUERY AND IS THEREFORE LOST\n- Map: apps/web/app/[locale]/map/page.tsx:65-75 (`parseView` of lng/lat/z), 86-92 (`mapFilterSchema.safeParse(sp)` for group/taxonId/from/to). components/map/HeatmapView.tsx:836-852 writes `lng`, `lat`, `z` plus `filterToQuery(filter)` into the address bar with `history.replaceState` on every moveend. The map page renders `SiteHeader` (map/page.tsx:106), which renders the switcher (components/site/SiteHeader.tsx:111 and 147).\n- Species directory: app/[locale]/(site)/species/(directory)/page.tsx:35, 39 reads `q` and `filter`; components/species/SpeciesSearch.tsx:29 writes `?q=` via `router.replace`.\n- Reports list: app/[locale]/(site)/reports/(list)/page.tsx:62-72 reads `page` and group/taxonId/from/to via `mapFilterSchema` (packages/shared/src/index.ts:182-193).\n- Report form: app/[locale]/(site)/report/page.tsx:23, 32 reads `category`.\n- NOT affected: the home page. app/[locale]/page.tsx imports no HeatmapView and reads no searchParams, so the switcher's doc comment about \"the transparent bar floating over the home page's map\" (LanguageSwitcher.tsx:14-16) is stale; nothing is lost on `/`.\n\nHTTP CHECKS (curl against localhost:3000)\n- `/species?q=deer` and `/en/species?q=deer` both render `value=\"deer\"` in the search input — the server honours `q` in both locales, so dropping it on switch empties the search.\n- `/en/map?lng=120.5&lat=23.5&z=9` serializes `initialView:{center:[120.5,23.5]…}` in the RSC payload; `/en/map` with no query serializes `initialView:null` — the view the switcher discards is exactly what the server would have restored.\n- `/en/reports?from=2024-01-01&to=2024-12-31` echoes `from=2024-01-01&to=2024-12-31` in its pagination/links, confirming the date filter is URL state.\n- `curl -I /zh-TW/species?q=deer` → `307`, `location: /species?q=deer` — the next-intl middleware (apps/web/proxy.ts:4) preserves the query on the default-locale strip, so a fix that carries the query will survive the extra redirect next-intl forces with `forcePrefix`.\n\nONE NUANCE FOR THE FIX: Next patches `window.history.replaceState` (node_modules/next/dist/client/components/app-router.js:235-268) so `useSearchParams()` would reflect HeatmapView's direct replaceState writes; I read that source but did not exercise it in a browser.",
   "fix": "Carry the current query string when switching locale, in apps/web/components/LanguageSwitcher.tsx.\n\nOption A (simplest, no Suspense needed): read the query at click time from the browser rather than via a hook:\n  onClick={() => router.replace(`${pathname}${window.location.search}`, { locale: l })}\nnext-intl's getPathname passes a string href through verbatim and applies the locale prefix (createSharedNavigationFns.js:74-86), and the middleware keeps the query on the resulting `/zh-TW/...` → `/...` 307 (verified by curl). This also picks up HeatmapView's `history.replaceState` writes directly, with no dependency on Next's history patch. Hash is not used by the map (no MapLibre `hash` option found), so `window.location.search` is sufficient.\n\nOption B (idiomatic): `const searchParams = useSearchParams()` from `next/navigation` and `router.replace({ pathname, query: Object.fromEntries(searchParams) }, { locale: l })` — getPathname serializes `href.query` (createSharedNavigationFns.js:71-73). Caveat: SiteHeader/SiteFooter render the switcher on the statically prerendered home page (app/[locale]/layout.tsx:10 generateStaticParams; app/[locale]/page.tsx has revalidate=300 and no searchParams), and no Suspense boundary wraps the header (grep of SiteHeader.tsx, SiteFooter.tsx, the locale/site layouts found none), so `next build` would fail with Next's \"useSearchParams() should be wrapped in a suspense boundary\" error unless the switcher's body is wrapped in `<Suspense>` (inference from Next's documented rule; not run).\n\nEither way: replace the stale comment at LanguageSwitcher.tsx:8-18 (no home-page map exists; the comment's \"keeps the reader where they were\" is true for the path but not for the state in the query), and add a Playwright case in apps/web/e2e/pages.spec.mjs that opens `/en/species?q=deer` (or `/en/map?lng=120.5&lat=23.5&z=9`), clicks 中文, and asserts the query survives. Consider `router.push` instead of `replace` so Back returns to the previous language with its state, though that is a separate judgement call."
  },
  {
   "id": "report-prefill",
   "area": "navigation",
   "verdict": "confirmed",
   "severity": "low",
   "effort": "hours",
   "evidence": "The claim and its stated reason are both accurate.\n\nSpecies page (apps/web/app/[locale]/(site)/species/[id]/page.tsx): the \"Seen one? Report it\" link at lines 260-267 is `<Link href=\"/report\">` with `t(\"seenOne\")` — a bare path, no query string, nothing carrying `s.id`. The zero-record variant \"Be the first to report one\" at lines 192-197 is the same bare `href=\"/report\"`. Strings: apps/web/messages/en.json:189-190 (\"Be the first to report one\", \"Seen one? Report it\"), zh-TW.json:189-190.\n\nReport page (apps/web/app/[locale]/(site)/report/page.tsx): `searchParams` is typed `Promise<{ category?: string }>` (line 23); lines 32-35 read only `category` and validate it against CATEGORY_KEYS; lines 43-46 pass ReportForm only `maptilerKey` and `initialCategory`. The comment at 29-31 says any other query key is ignored.\n\nForm (apps/web/components/report/ReportForm.tsx): the props type at lines 30-41 has exactly `maptilerKey?` and `initialCategory?`. Species state is `useState<SpeciesHit | null>(null)` at line 53 and is fed to SpeciesPicker at 324-330 — there is no prop that could seed it.\n\nHTTP check: `curl -L localhost:3000/species/32116` → 200 at /species/32116-prionailurus-bengalensis; every href containing \"report\" is `href=\"/report\"` (x2) or `href=\"/reports\"`; none has a query. /en/species/32116 → 2x `href=\"/en/report\"` — one is the site header button (apps/web/components/site/SiteHeader.tsx:115-119), the other is the species page's line 262. Only the front page's category doors ever add a query to /report (apps/web/app/[locale]/page.tsx:233, `/report?category=${d.key}`).\n\nThe repo already has the exact pattern the report page lacks, on the map: apps/web/app/[locale]/map/page.tsx:34-45 `namedTaxon(id)` resolves a SpeciesHit-shaped row server-side, line 97-98 `initialSpecies = taxonId ? await namedTaxon(taxonId) : null`, and apps/web/components/map/MapFilters.tsx:31-48 seeds `useState(initialSpecies)`. SpeciesPicker (apps/web/components/report/SpeciesPicker.tsx:8-14 SpeciesHit = {id, scientificName, commonNameZh, isInvasive, reportCount}; lines 102-130) already renders a chosen species as a chip with a \"change\" button whenever `value` is non-null, so it would display a prefilled value with no changes.\n\nWhat the code does: a visitor clicking either link lands on the form with category defaulting to \"roadkill\" and the species field empty. Inferred, not observed: since the species field is optional (SpeciesPicker.tsx:136 label says optional), a visitor who came from a species page and skips retyping the name files a report with `taxonId` undefined.",
   "fix": "Three small edits, following the map page's existing pattern:\n\n1. apps/web/app/[locale]/(site)/species/[id]/page.tsx lines 193 and 262: change `href=\"/report\"` to `href={`/report?taxonId=${s.id}`}` (the i18n Link already handles the locale prefix, as the front page's `/report?category=` link shows).\n\n2. apps/web/app/[locale]/(site)/report/page.tsx: widen `searchParams` to `{ category?: string; taxonId?: string }`; parse taxonId as a positive safe integer (reuse `parseSpeciesId` from @/lib/species or a Number check); resolve it with `getSpecies(id)` from @/lib/species (not a copy of the map's `namedTaxon`, which inner-joins species_report_stats and so returns null for a taxon with zero public reports — exactly the \"Be the first\" case; `getSpecies` uses `coalesce(s.report_count, 0)` and is what the species page itself renders zero-report taxa with). Map the result to SpeciesHit {id, scientificName, commonNameZh, isInvasive, reportCount} and pass `initialSpecies` to ReportForm. An invalid or unknown id should be dropped silently, matching the page's existing stance on a bad ?category= (lines 29-31).\n\n3. apps/web/components/report/ReportForm.tsx: add `initialSpecies?: SpeciesHit | null` to the props (lines 30-41) and change line 53 to `useState<SpeciesHit | null>(initialSpecies ?? null)`. SpeciesPicker needs no change.\n\nOptional: when the prefilled species `isInvasive` and no ?category= was given, default the category to \"invasive\" so the group button matches the animal; otherwise leave the \"roadkill\" default. Add a case to whatever test covers ?category= prefill on the report page, and a page test that /report?taxonId=<zero-report id> still prefills."
  },
  {
   "id": "tiny-text",
   "area": "accessibility",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "day",
   "evidence": "Every number in the claim is literally in the code, and the served HTML/CSS from localhost:3000 confirms it reaches the browser unchanged.\n\nBADGES 10px — apps/web/components/species/StatusBadges.tsx:141 `className={\\`rounded px-1.5 py-0.5 text-[10px] font-medium ring-1 ring-inset ${c.tone}\\`}`. No responsive variant. Rendered at app/[locale]/(site)/species/[id]/page.tsx:164, species/(directory)/page.tsx:118 and components/species/SpeciesCard.tsx:77. curl http://localhost:3000/species/32116 -> 307 -> /species/32116-prionailurus-bengalensis (200); the served page has six `<li … text-[10px] …>` badges: 保育類 珍貴稀有野生動物 / CITES 附錄一 / CITES 附錄二 / IUCN 暫無危機 / 臺灣紅皮書 國家瀕危 / 敏感物種. Note the default locale is zh-TW, so these are multi-stroke CJK strings at 10px, not Latin abbreviations. The same 10px chip pattern recurs at app/[locale]/(site)/me/page.tsx:330, components/admin/ModerationRow.tsx:62, components/map/MapFilters.tsx:145, components/site/SiteHeader.tsx:162, stats/page.tsx:228 and :278, and the whole footer bar is 10px (components/site/SiteFooter.tsx:56).\n\nCHART LABELS 9px — components/stats/Columns.tsx:46 `<span className=\"truncate text-[9px] tabular-nums text-ink-500\">` and components/species/MonthlyChart.tsx:30 `<span className=\"text-[9px] tabular-nums text-ink-500\">`. Columns is called from stats/page.tsx:164 and :180. curl /stats (200, 138 KB) contains 42 `text-[9px]` spans whose text is the digits 1–12 and the years 2011–2017. The species page has the 12 MonthlyChart digits at 9px and its hint at 11px (MonthlyChart.tsx:34: \"共 22 筆，1 月最多。\"). Also 9px: stats/page.tsx:208,213 (chips) and the homepage tagline at app/[locale]/page.tsx:200 (`text-[9px]` below sm, 13px above); components/brand/Wordmark.tsx:49 goes down to `text-[8px]`.\n\nPHOTO-REMOVAL BUTTON 20px — components/report/ReportForm.tsx:356 `className=\"absolute -right-1.5 -top-1.5 grid h-5 w-5 place-items-center rounded-full bg-paper-200 text-xs …\"`, aria-label at :357, glyph \"×\" at :359. Compiled CSS (/_next/static/chunks/apps_web_app_globals_1lkgh9-.css) has `.h-5 { height: calc(var(--spacing) * 5) }` (line 796), `.w-5` likewise (896), `--spacing: .25rem`, `--text-xs: .75rem`, and no html/:root font-size override anywhere (globals.css:103-110 sets only height/background/color), so the button is 20×20 CSS px with a 12px \"×\". It is client-rendered only after a photo is added, so it does not appear in SSR HTML; the size is arithmetic, not inference. The photo tiles sit in `flex flex-wrap gap-2` (:341), 80×80 images (:352), \"+\" picker 80×80 (:369).\n\nINSTRUCTIONS 10–12px — repo-wide count (apps/web, excluding node_modules/.next): 59× text-[11px], 14× text-[13px], 11× text-[10px], 9× text-[12px], 6× text-[9px], 1× text-[8px], plus 68× text-xs (=12px) and 82× text-sm (=14px). In ReportForm.tsx the helper/instruction copy is 11px at :391 (photoHelp/photoProcessing), :396 (noPhotoWarning), :425 (EXIF offer, including its two buttons), :458 (tapToAdjust + accuracy), :521 (emailHelp); and 12px (text-xs) at :220, :234, :239, :282 (type chips), :310, :418 (use-my-location button), :526 (error), :544 (blocker). curl /report (200) serves 15 `text-[11px]` and 34 `text-xs` occurrences. Also globals.css:155 and :171 set MapLibre's attribution and scale bar to `font-size: 10px`.\n\nWhat is NOT wrong: contrast. globals.css:33-35 documents ink-500 at 5.6:1 / 5.1:1 on paper-50/100, so the small text is not also a contrast failure. px sizes still scale with browser zoom (WCAG 1.4.4 is met); they do not follow the user's default-font-size preference the way rem would.\n\nOn the review's reasoning: the sizes are exactly as stated. WCAG has no minimum font-size criterion, so if the review frames the text sizes as a WCAG failure that framing is loose; the real problems are (a) 9–10px CJK glyphs for the default zh-TW audience, which most Chinese-typography guidance puts at 12px minimum, and (b) the 20px remove control, which is under WCAG 2.2 SC 2.5.8's 24×24 minimum (the spacing exception is borderline: a 24px circle centred on the button, which overhangs the image by 6px, reaches exactly the 8px gap to the \"+\" tile) and far under the 44pt/48dp platform guidance on a form that is explicitly phone-first (`capture=\"environment\"` at :373). Minor related note: Columns.tsx:4-7 says the \"real content is the table\" for screen readers, but there is no table; values exist only as `title` on a div (:44), so the 9px labels plus hover tooltips are the only visible path to the numbers.",
   "fix": "Targeted (the three named spots): StatusBadges.tsx:141 change `text-[10px]` to `text-xs` (12px) and `px-1.5` to `px-2`; Columns.tsx:46 and MonthlyChart.tsx:30 change `text-[9px]` to `text-[11px]` or `text-xs` (12 columns with gap-1 at ~400px gives ~28px per column, so 2-digit and even 4-digit year labels fit; keep `truncate` on Columns and consider hiding alternate year labels at narrow widths); ReportForm.tsx:356 change `h-5 w-5 text-xs` to at least `h-6 w-6` (24px, meets SC 2.5.8) and preferably give it a 44px hit area (e.g. `h-7 w-7` plus `before:absolute before:-inset-2`) with the glyph at `text-sm`, and re-check `-right-1.5 -top-1.5` so the larger disc still clears the neighbouring \"+\" tile.\n\nSystemic: add sized tokens in the `@theme` block of apps/web/app/globals.css (e.g. `--text-2xs: 0.6875rem` = 11px, and treat `text-xs` 12px as the floor for any readable text) and sweep the ~85 `text-[8-12px]` sites (list in evidence; heaviest are app/[locale]/page.tsx, stats/page.tsx, me/page.tsx, ReportForm.tsx, QueueBanner.tsx, SiteFooter.tsx) so sizes are rem-based and centrally adjustable. Promote form instructions (ReportForm.tsx:391,396,458,521 and the QueueBanner/SpeciesConfirm equivalents) to `text-xs`/`text-sm` with `leading-relaxed`. Bump globals.css:155 and :171 to 11–12px. Optionally add a `sr-only` data table under Columns/MonthlyChart so the comment at Columns.tsx:4-7 becomes true."
  },
  {
   "id": "dark-tokens-on-light",
   "area": "accessibility",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "All four facts in the claim are true in the code at c73bb8d; three carry a real contrast problem, the fourth is a cosmetic inconsistency rather than an accessibility one.\n\nDESIGN RULE THE CLAIM IS TESTED AGAINST: apps/web/app/globals.css:11-20 states pages are light (\"paper\"/\"ink\") and \"bark\"/\"parchment\" are for the map and its chrome, \"a component belongs to one or the other\". html/body are painted paper-50 (#faf7f0) at globals.css:103-110. The report page is a plain `<main>` on that ground (apps/web/app/[locale]/(site)/report/page.tsx:38), and curl http://localhost:3000/report returns `<html lang=\"zh-TW\"><body class=\"h-full antialiased\">` with no dark class.\n\n1. EXIF offer — TRUE, and it is a leftover from the old dark UI. apps/web/components/report/ReportForm.tsx:425 `border-sky-500/30 bg-sky-500/10 ... text-sky-200`; :430 the \"Use\" button `bg-sky-400/20` inherits sky-200; :441 the \"Skip\" button `text-sky-300/70`. `sky` is not a project token at all (globals.css @theme :22-97 defines paper/ink/bark/parchment/scale/ember/moss only); it is Tailwind's default palette. The dev server's compiled stylesheet (/_next/static/chunks/apps_web_app_globals_1lkgh9-.css) emits `--color-sky-200: #b8e6fe`, `.bg-sky-500/10 { background-color: #00a5ef1a }`, `.text-sky-300/70 { color: #77d4ffb3 }`. Computed over paper-50: offer background #e1eff0; sky-200 text on it 1.13:1; \"Use\" button text 1.02:1; \"Skip\" text 1.28:1 (WCAG AA needs 4.5:1 for 11px text). `git log -S'text-sky-200'` shows it was introduced in the initial commit c5647de (2026-08-05), before the light-page redesign eae507d (2026-08-06) — the header comment at globals.css:7-9 describes exactly that prior \"slate-blue\" UI. The strip is reachable by a real visitor: it appears whenever an uploaded photo carries GPS EXIF and no location is set yet (ReportForm.tsx:104-107).\n\n2. Coordinate text — TRUE, and the review's reason (parchment is a dark-ground token) matches the design's own rule. apps/web/components/report/LocationPicker.tsx:127 `text-[11px] text-parchment-400 tabular-nums` on a `<p>` that is a sibling BELOW the map container (:118-125), i.e. on paper, not over the map. `--color-parchment-400: #9d9179` (globals.css:64) on paper-50 = 2.90:1 (2.65:1 on paper-100). The sibling caption two lines later in ReportForm.tsx:458 uses `text-ink-500` (5.57:1), so the light-page token was already at hand. Note this one is NOT a leftover: it was written during the redesign commit eae507d.\n\n3. color-scheme — TRUE. globals.css:99-101 `:root { color-scheme: dark; }` while :103-110 paints html/body paper-50; the compiled CSS ships it (line 3280 of the emitted file). Both landed in c5647de. What the code does: it tells the UA to render native control chrome dark. Inferred (not screenshotted, per the no-browser rule): the checkbox at apps/web/components/report/SpeciesPicker.tsx:227, the datetime-local picker/indicator at ReportForm.tsx:477-484, the search input's clear button at SpeciesPicker.tsx:140 and apps/web/components/species/SpeciesSearch.tsx:37, `<select>` popups, and scrollbars would draw in dark UA styling on the cream page. On the map it is arguably wanted: MapFilters' `<option className=\"bg-bark-900\">` at apps/web/components/map/MapFilters.tsx:283 and :297 shows dark dropdowns are intended there.\n\n4. Focus ring — TRUE as a code fact, but the effect is cosmetic, not an accessibility failure. globals.css:238-248 `input:focus, textarea:focus, select:focus, ... { box-shadow: 0 0 0 2px var(--color-paper-50), 0 0 0 4px var(--color-ember-500); border-color: transparent }` — the inner \"gap\" halo is hard-coded to paper-50. Inputs that sit on the dark map: the species search `<input>` at MapFilters.tsx:210-221 and the two year `<select>`s at :275-300, inside the panel `bg-bark-900/92` (:152) mounted over the map at apps/web/components/map/HeatmapView.tsx:1001-1008. There the cream halo is 17.5:1 against bark-950 and ember-500 is 5.45:1 — i.e. the ring is loud, not invisible. The mismatch is with the MapLibre button ring at globals.css:210-217, which uses bark-950 as the halo; two different rings share the same dark surface. On paper, ember-500 vs paper-50 is 3.21:1, which clears SC 1.4.11's 3:1 for a non-text indicator.",
   "correction": "Items 1-3 are true for the reason the review gives. Item 4 is true (the shared rule hard-codes a paper-50 halo and three controls do sit on the dark map) but its accessibility framing is wrong: on the map the ring is highly visible (17.5:1 halo, 5.45:1 ember), so the problem is visual inconsistency with the MapLibre control ring, not legibility. Also, item 2 is not a legacy leftover like items 1 and 3; it was written in the redesign commit itself.",
   "fix": "1. apps/web/components/report/ReportForm.tsx:425,430,441 — replace the sky-* classes with page tokens, e.g. container `border-ink-900/12 bg-paper-100/70 text-ink-700` (ink-600 on the tinted strip is 6.27:1), \"Use\" button `bg-ember-500/15 text-ember-700` (ember-700 is the accent step globals.css:42-45 says passes on paper), \"Skip\" `text-ink-500`.\n2. apps/web/components/report/LocationPicker.tsx:127 — `text-parchment-400` -> `text-ink-500`, matching the caption at ReportForm.tsx:458.\n3. apps/web/app/globals.css:99-101 — `:root { color-scheme: light }`, then scope `color-scheme: dark` to the map chrome (the MapFilters panel wrapper at apps/web/components/map/MapFilters.tsx:152, or the HeatmapView filter overlay at apps/web/components/map/HeatmapView.tsx:1000) so the year `<select>` popups and search input keep their dark native chrome.\n4. apps/web/app/globals.css:238-248 — replace `var(--color-paper-50)` with a custom property, e.g. `--focus-halo: var(--color-paper-50)` on :root and `--focus-halo: var(--color-bark-950)` on the same map-chrome scope as item 3, and reuse it in the MapLibre button ring at :210-217 so both rings agree.\nVerify by re-running the contrast arithmetic on the new pairs and tabbing through /report and the map filters."
  },
  {
   "id": "login-a11y",
   "area": "accessibility",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "All three parts of the claim are true, and the review's reasons are correct.\n\n1. No label. apps/web/app/[locale]/(site)/login/page.tsx:44-50 renders `<input type=\"email\" value={email} onChange placeholder=\"you@example.com\" className=…>` with no `id`, no `<label>`, no `aria-label`, no `aria-labelledby`. `PageHeader` (apps/web/components/site/PageHeader.tsx:22-33) only emits an `<h1>` and a `<p>`; nothing associates them with the input. Served HTML confirms it: `curl http://localhost:3000/login` and `/en/login` both return the `<main>` with `<label>=0, aria-label=0, aria-labelledby=0` inside it; the input is `<input type=\"email\" placeholder=\"you@example.com\" class=\"…\" value=\"\"/>`. One nuance: browsers fall back to `placeholder` for the accessible name, so a screen reader will announce \"you@example.com\" rather than nothing — but that is an example value, not a label, and it disappears once the user types. There is no visible label at all (WCAG 3.3.2 / 1.3.1 failure).\n\n2. Button click, not form submit. page.tsx:43 wraps the controls in `<div className=\"space-y-3\">`, not `<form>`; page.tsx:52-54 is `<button type=\"button\" onClick={send}>`; there is no `onKeyDown`/`onSubmit` anywhere in the file. Served HTML for both locales: `<form>=0`. An `<input>` outside a `<form>` has no implicit submission, so pressing Enter does nothing. The whole codebase has no `<form` tag at all (grep of apps/web/app, components, lib), so this isn't an isolated omission but it is the page where it bites hardest — a single-field sign-in where nearly everyone presses Enter.\n\n3. No resend / change-email. page.tsx:38-42: once `sent` is true the page renders only `<p>{t(\"sent\")}</p>`; `sent` is never reset, the input and button are unmounted, and no control is rendered. The only recovery is a full reload. The `login` message namespace in apps/web/messages/en.json:172 and zh-TW.json:172 has exactly five keys (heading, explain, send, sending, sent) — no resend/change strings exist to wire up. (On the error path, page.tsx:51, the input stays mounted so a retry is possible; the gap is only after success.)\n\nRelated gap the review did not mention: apps/web/app/auth/callback/route.ts:16 and :22 redirect a failed or expired magic link to `/login?error=missing_code` / `/login?error=exchange_failed`, but the login page never reads `searchParams`/`useSearchParams` (grep confirms no reader anywhere), so the visitor lands on a pristine login page with no explanation. The error `<p>` at page.tsx:51 also has no `role=\"alert\"`/`aria-live`, so screen readers are not told when the send fails.",
   "fix": "All in apps/web/app/[locale]/(site)/login/page.tsx plus two message files:\n\n- Replace the `<div className=\"space-y-3\">` (page.tsx:43) with `<form onSubmit={(e) => { e.preventDefault(); send(); }} noValidate>` and change the button (page.tsx:53) to `type=\"submit\"`. Keep the existing `disabled` guard; Enter then submits via the button.\n- Add a visible `<label htmlFor=\"login-email\">` above the input using the same pattern already in apps/web/components/report/ReportForm.tsx:506-519, and give the input `id=\"login-email\" name=\"email\" autoComplete=\"email\" required`, plus `aria-invalid={!!error}` and `aria-describedby=\"login-error\"` when an error is shown. Add `login.emailLabel` to apps/web/messages/en.json and zh-TW.json.\n- Give the error `<p>` (page.tsx:51) `id=\"login-error\" role=\"alert\"`.\n- In the `sent` branch (page.tsx:38-42), show the address it was sent to and two buttons: \"Resend\" (calls `send()` again; Supabase rate-limits OTP resends, so either disable it for ~60 s or surface the rate-limit error message) and \"Use a different email\" (`setSent(false)`; keep `email` in state so the field is prefilled). Add `login.sentTo`, `login.resend`, `login.changeEmail` keys in both locales.\n- Optional but cheap: read `error` with `useSearchParams()` and map `missing_code` / `exchange_failed` (from apps/web/app/auth/callback/route.ts:16,22) to a message shown above the form, so an expired link explains itself instead of silently landing on the form."
  },
  {
   "id": "chart-a11y",
   "area": "accessibility",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "apps/web/components/stats/Columns.tsx:29 and apps/web/components/species/MonthlyChart.tsx:22 wrap the bars in `<div role=\"img\" aria-label={label}>`; role=\"img\" makes descendants presentational, so the x-axis label spans (Columns.tsx:46-48, MonthlyChart.tsx:30) and the per-bar `title` attributes (Columns.tsx:44 `title={\\`${d.key}: ${d.n.toLocaleString()}\\`}`, MonthlyChart.tsx:28 `title={\\`${i + 1}: ${n}\\`}`) are not exposed to assistive tech. Neither component contains a table, aria-describedby, or sr-only value list; curl of /stats, /en/stats and /en/species/28758-duttaphrynus-melanostictus confirms `any <table>?: False` inside every <figure>. The bar divs are not focusable, so values are mouse-hover only (no keyboard/touch path). apps/web/app/[locale]/(site)/stats/page.tsx:80-84 builds month keys with `Intl.DateTimeFormat(locale, { month: \"narrow\" })` and passes them to Columns at lines 164-168; rendered /en/stats shows x labels ['J','F','M','A','M','J','J','A','S','O','N','D'] and hover titles 'M: 1,542' / 'M: 4,855' / 'J: 5,702' / 'J: 6,578' — the tooltips are ambiguous as well as the axis. Columns.tsx:31-32 comments on exactly this collision but only fixes the React key. The doc comment at Columns.tsx:4-7 says the chart is screen-reader readable \"because the real content is the table ... see the caption and per-bar titles\" — there is no table, the sr-only figcaption (Columns.tsx:28) is just the title, and titles inside role=\"img\" are never announced. Scope limits: zh-TW default renders 1..12 (curl /stats x labels ['1',...,'12']); \"By year\" (page.tsx:179-184) uses 4-digit years; MonthlyChart.tsx:30 uses `{i + 1}` numerals in both locales, so the J/F/M ambiguity is English-only and stats-page-only. Partial text fallbacks exist: page.tsx:170-176 \"Peaks in July.\" and MonthlyChart.tsx:34-36 \"3,978 records; peak in month 4.\" — one/two values, not the series. The \"By year\" chart has no text fallback at all. Bars.tsx:31-34 does expose values as visible text but is gated by `cats.length > 1` at page.tsx:148 and is not rendered on current single-category data.",
   "correction": "All three parts of the claim are true, but the narrow-month ambiguity applies only to the stats \"By month\" chart in the en locale: zh-TW (the default) renders 1..12, the year chart uses years, and MonthlyChart uses numerals 1..12 in every locale. Both month charts do carry a one-line visible summary (peak month / total), so screen-reader users get the chart name plus that sentence rather than nothing; the \"By year\" chart has no summary at all. The review's reasoning is correct; it is the code's own header comment (Columns.tsx:4-7, claiming an accessible table exists) that is wrong.",
   "fix": "1) apps/web/components/stats/Columns.tsx and apps/web/components/species/MonthlyChart.tsx: keep the role=\"img\" div for the visual but add a `<table className=\"sr-only\">` (caption = label, one row per key/value) inside the <figure> after it, and set aria-describedby on the role=\"img\" div to the table's id; this exposes every value to screen readers and, being real DOM text, also serves keyboard/touch users if the sr-only class is dropped on focus or the table is shown in a <details>. Alternatively replace role=\"img\" with a <ul> where each <li> has the visible key and an sr-only value. 2) apps/web/app/[locale]/(site)/stats/page.tsx:80-84: use `month: \"short\"` (Jan..Dec in en, 1月..12月 in zh-TW) for the axis key, and pass a separate `month: \"long\"` name into the data so the hover title and the sr-only table read \"May: 4,855\" instead of \"M: 4,855\". 3) Delete or rewrite the misleading doc comment at Columns.tsx:4-7 once the table actually exists. 4) Minor while there: Columns.tsx:44 calls toLocaleString() with no locale and MonthlyChart.tsx:28 prints raw n; pass `locale` as Bars.tsx already does."
  },
  {
   "id": "legend-heat",
   "area": "map",
   "verdict": "confirmed",
   "severity": "low",
   "effort": "hours",
   "evidence": "The layer: apps/web/components/map/HeatmapView.tsx:601-676 adds HEAT_LAYER with `type: \"heatmap\"` (MapLibre kernel density). Its colour is driven by `[\"heatmap-density\"]` (a 0-1 normalised pixel value) through a 5-stop interpolate at lines 636-651; weight is `[\"get\",\"weight\"]` capped at 300 (612-623) and intensity is zoom-scaled (625-633). The code's own comment at 596-600 says the colour at a pixel \"is not any feature's count\".\n\nThe legend: lines 1032-1117. It branches only on `colour === \"type\"` (1037) and `mode === \"dots\"` (1088, 1093). There is no `mode === \"heat\"` branch. In heat+density it renders `DENSITY_CLASSES.map(...)` (1074) with labels from `densityClassMax` (packages/shared/src/index.ts:377-384), i.e. 1, 3-9, 10-29, 30-99, 100-299, 300+, as 10px square swatches using each class's hex (1085-1104), under the heading `t(\"map.density\")` (1033) and the caption `t(\"map.perCell\")` (1116) = \"reports per cell\" (apps/web/messages/en.json:54) / \"每格通報數\" (zh-TW.json:54). The mode effect at 874-897 only flips layer visibility and never touches the legend.\n\nTwo further mismatches the review did not mention: (1) the swatch hexes (index.ts:339-354: #1e3a5f, #1d4ed8, #0891b2, #10b981, #f59e0b, #dc2626) are not the heatmap-color stops (641-650: rgba(56,132,255), (34,211,238), (52,211,153), (251,146,60), (244,63,94)); six classes vs five stops, only the hue order matches despite the \"same progression\" comment at 634-635. (2) MapColourToggle is rendered unconditionally (1026-1030) but the colour effect at 899-916 repaints only CELL_LAYER and DOT_LAYER, so choosing \"Type\" in heat mode swaps the legend to type swatches while the heat layer stays density-coloured.\n\nHistory: the comment at 1036-1040 (\"Swatches with real counts, not a gradient bar\") explains the gradient's removal; that reasoning is right for bins/dots but was applied to heat too. `map.low`/`map.high` (en.json:44-45) are leftovers with no remaining uses (grep across apps/web found none). `git show fb8e44c` (the commit that introduced the three modes) already carries the same comment and `map.perCell` caption, so this has existed since heat mode shipped.\n\nHTTP: `curl http://localhost:3000/en/map` -> 200; SSR HTML has \"Dots\" aria-pressed=true (server default, apps/web/components/map/mapMode.ts:52-54) and \"reports per cell\" present. Heat mode is client-only localStorage state, so curl cannot show the heat legend; the finding rests on the code read above, which is unambiguous.\n\nNot affected: apps/web/components/species/SpeciesMap.tsx:262-278 renders a mode toggle but no legend at all.",
   "fix": "In apps/web/components/map/HeatmapView.tsx: (a) lift the heatmap-color stops at 636-651 into a `HEAT_STOPS` constant and build both the paint expression and a legend `linear-gradient` from it, so they cannot drift; (b) add a `mode === \"heat\"` branch in the legend (around 1073) that renders that gradient bar with the existing unused `map.low`/`map.high` labels (en.json:44-45, and the zh-TW equivalents), and swap the caption at 1116 to a new key such as `map.relativeDensity` (\"Relative density, smoothed — not a count\") added to both apps/web/messages/en.json and zh-TW.json; (c) in heat mode either hide MapColourToggle (1026-1030) or force the legend to ignore `colour`, since the heat layer does not respond to the colour axis (899-916). Optionally derive HEAT_STOPS from DENSITY_CLASSES colours so the \"same progression\" comment at 634-635 becomes literally true."
  },
  {
   "id": "legend-type-heat",
   "area": "map",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "The claim and its stated reason are both correct.\n\nWhat the code does (read directly):\n- apps/web/components/map/HeatmapView.tsx:899-917 — the colour effect (`useEffect` keyed on `[colour, ready]`) makes exactly two `setPaintProperty` calls: CELL_LAYER \"fill-color\" (907-911) and DOT_LAYER \"circle-color\" (912-916). HEAT_LAYER is never touched. The comment at 903-906 explains why POINT_LAYER is skipped (\"one report has no density to express\"); the heat layer's omission is not mentioned anywhere.\n- HeatmapView.tsx:602-670 — HEAT_LAYER is created with a hard-coded \"heatmap-color\" ramp over [\"heatmap-density\"] (636-651, blue->cyan->green->amber->red). Unlike CELL_LAYER (576: `byType ? groupColor : densityStep`) and DOT_LAYER (683: `byType ? groupColor : dotColor`), it has no `byType` branch at creation either.\n- HeatmapView.tsx:874-896 — the mode effect only flips \"visibility\" on the three layers; it does not reconcile colour.\n- HeatmapView.tsx:1026-1030 — `<MapColourToggle colour onChange>` is rendered with no knowledge of `mode`. The legend header (1032-1034), body (1041: `colour === \"type\" ? <type list> : <density classes>`) and caption (1116: `t(\"map.typePerCell\")`) all branch on `colour` alone, never on `mode`.\n- apps/web/components/map/MapColourToggle.tsx:14-45 — takes only `colour`/`onChange`/`className`; renders both COLOURS unconditionally, no disabled state.\n- apps/web/components/map/mapMode.ts — `colourStore` persists to localStorage (COLOUR_KEY), so heat+Type, once chosen, survives reloads and applies on every visit. Defaults are dots + density (getServer/get fallbacks).\n- apps/web/messages/en.json:70,72 — in that state the legend reads \"Report type\" with three group swatches plus grey \"Mixed\", captioned \"Whichever type most of the cell's reports are.\" — over a heat surface that has no cells and is coloured by density.\n- Only HeatmapView consumes colourStore (grep across apps/web found no other consumer), so the species maps are unaffected.\n- No test pins this: apps/web/test/map-bins.test.mjs checks layer types and MODES but nothing about the colour axis; apps/web/e2e has no legend/colour coverage.\n\nHTTP check: `curl http://localhost:3000/en/map` -> 200 (66,967 bytes). The SSR HTML contains both toggles (Heat/Grid/Dots with Dots pressed; Density/Type with Density pressed) and the caption \"reports per cell\". Because the combination is client state, the mismatched rendering itself is inferred from the code above, not observed in a browser (headless browsers were off-limits for this check).\n\nTechnical note on the cause: MapLibre's `heatmap-color` is a ramp over `heatmap-density` and cannot be data-driven per feature, so a single heatmap layer cannot be recoloured by `top_group` the way the fill and circle layers are. The omission is structural, but the UI does nothing to reflect that — nothing disables \"Type\" in heat mode and the legend does not fall back to density.",
   "fix": "Cheapest honest fix (treat heat as density-only):\n1. apps/web/components/map/HeatmapView.tsx — derive `const legendColour = mode === \"heat\" ? \"density\" : colour;` and use it instead of `colour` at the legend header (~1033), body branch (~1041) and caption (~1116). The colour effect at 899-917 can stay as is (it already only affects cell/dot layers).\n2. apps/web/components/map/MapColourToggle.tsx — add an optional `disabled?: readonly MapColour[]` (or `disabledReason`) prop; render the \"type\" button with `disabled`/`aria-disabled` and a `title` explaining \"not available for heat\" when the parent passes it. HeatmapView passes `disabled={mode === \"heat\" ? [\"type\"] : []}`. Keep the stored preference so switching back to Grid/Dots restores Type without the user re-picking it.\n3. Add a source-pinning assertion in apps/web/test/map-bins.test.mjs alongside the existing MODES checks, e.g. that the legend key is derived from `mode === \"heat\" ? \"density\" : colour`, so the combination cannot silently regress.\n\nFuller alternative (heat by type): the `reports_dots` tile layer already carries `top_group` and `top_share` (apps/web/app/api/tiles/[z]/[x]/[y]/route.ts:198,210), so one heatmap layer per REPORT_GROUP_KEYS could be added, each filtered by `[\"==\", [\"get\",\"top_group\"], g]` (and `top_share >= MIXED_BELOW`) with a ramp from transparent to that group's CATEGORIES colour, and the colour effect would flip visibility between the density heat layer and the per-group set. Overlapping KDEs blend and get muddy where groups co-occur, and the legend's \"Mixed\" entry has no heat counterpart, so this needs design judgement; it is a day or more, versus hours for the first option."
  },
  {
   "id": "legend-points",
   "area": "map",
   "verdict": "partly",
   "severity": "medium",
   "effort": "hours",
   "evidence": "CORE OF THE CLAIM IS TRUE. apps/web/components/map/HeatmapView.tsx:709-721 adds POINT_LAYER (type \"circle\", source SOURCE_PTS) with `\"circle-color\": categoryColor` (line 721). `categoryColor` (lines 357-362) is a plain `[\"match\", [\"get\",\"category\"], ...CATEGORY_KEYS -> CATEGORIES[k].color, \"#94a3b8\"]` with no zoom and no colour-mode input. The colour-toggle effect (lines 898-916) deliberately only calls setPaintProperty on CELL_LAYER and DOT_LAYER; its own comment at lines 905-907 says: \"The individual points at high zoom are not touched: one report has no density to express, so they are coloured by type in both settings.\" So the map behaviour is intentional, not a bug in the paint.\n\nThe legend does NOT follow that decision. Lines 1030-1112: heading is `colour === \"type\" ? t(\"map.reportType\") : t(\"map.density\")` (line 1031), body is DENSITY_CLASSES swatches with numeric ranges 1 / 3–9 / 10–29 / 30–99 / 100–299 / 300+ (lines 1071-1107), caption `t(\"map.perCell\")` = \"reports per cell\" / \"每格通報數\" (line 1110; apps/web/messages/en.json:54, zh-TW.json:54). The legend reads only `colour` and `mode`; nothing zoom-related reaches it. The only zoom-driven UI state is `hint` (zoomend handler, lines 813-818), which clears at z >= 13.5 and does nothing to the legend. Default colour is \"density\" (apps/web/components/map/mapMode.ts:81-88, getServer returns \"density\"), default mode \"dots\", so a first-time visitor who follows the \"Zoom in for individual records\" hint lands exactly in this state.\n\nTHE ZOOM NUMBER IN THE CLAIM IS WRONG. packages/shared/src/index.ts:315 `export const TILE_AGGREGATION_MAX_ZOOM = 13;`. POINT_LAYER minzoom = TILE_AGGREGATION_MAX_ZOOM + 1 = 14 (HeatmapView.tsx:716); SOURCE_PTS minzoom is also 14 (line 553). Cells/dots fade 0.85@z13 -> 0.55@z14 -> 0@z14.2 (lines 581-592, 689-700). So the regime where points are drawn under a Density legend is z >= 14 (crossfade z14–14.2), not z >= 10. Between z10 and z13.99 the map still draws aggregated cells/dots coloured by the density step, and the legend is correct there. The reviewer was almost certainly misled by stale comments that still say z10: HeatmapView.tsx:735-737 (\"cells that stop dead at z10\") and packages/shared/src/index.ts:308-314 (\"Set to 10 rather than 9 ... measured on the densest z10 tile\") directly above a constant whose value is 13.\n\nTHE CLAIM ALSO UNDERSTATES IT: the two colour schemes collide. CATEGORIES (packages/shared/src/index.ts:13-18): roadkill #e11d48, invasive #f59e0b, injured #fb923c, sighting #10b981. DENSITY_CLASSES (index.ts:339-352): 30–99 = #10b981, 100–299 = #f59e0b, 300+ = #dc2626. So at z >= 14 with the default Density legend, a single wildlife sighting reads as \"30–99 reports per cell\", a single invasive-species report as \"100–299\", and every roadkill point (#e11d48, and the dataset is essentially all roadkill) reads as the \"300+\" hotspot class. The legend is not merely stale; it assigns a specific wrong number to every dot on screen.\n\nHTTP CHECK (curl against localhost:3000, tiles are gzip-encoded, decoded with @mapbox/vector-tile): GET /api/tiles/13/6860/3507 -> 200, layers `reports` (26 cells) + `reports_dots`, properties {weight, top_group, top_share}, no `category`. GET /api/tiles/14/13721/7014 -> 200, layer `reports` with 13 point features each carrying `category: \"roadkill\"`, id, taxon_id, observed_on, weight=1 (matches the point SQL at apps/web/app/api/tiles/[z]/[x]/[y]/route.ts:218-234, regime switch `aggregated = z <= TILE_AGGREGATION_MAX_ZOOM` at line 114). So `categoryColor` resolves to real category colours at z14, not the grey fallback; the behaviour described above is what a visitor sees, not an inference.\n\nSecondary, in \"Type\" colour mode at z >= 14: the legend's roadkill swatch uses CATEGORIES[categoriesIn(g)[0]].color = #e11d48 (HeatmapView.tsx:1047) and its caption \"Whichever type most of the cell's reports are.\" (line 1110, en.json:72); injured points are painted #fb923c which matches no legend swatch, and there are no cells. Smaller issue, same root cause: the legend has no notion of the point regime.",
   "correction": "The mismatch exists, but it starts at z >= 14 (POINT_LAYER minzoom = TILE_AGGREGATION_MAX_ZOOM + 1, with the constant = 13), not z >= 10; from z10 to z13.99 the map still shows density-coloured cells/dots and the legend is correct. The \"z10\" figure comes from stale code comments, not from the code. The claim also understates the problem: because CATEGORIES and DENSITY_CLASSES reuse the same hex values (#10b981, #f59e0b) and near-identical reds, the Density legend at z >= 14 assigns a specific wrong count to every single-report dot (a sighting reads as 30–99, invasive as 100–299, roadkill as 300+).",
   "fix": "Make the legend follow the map's regime rather than changing the paint (the paint choice is deliberate and documented at HeatmapView.tsx:905-907).\n\n1. apps/web/components/map/HeatmapView.tsx: add `const [pointsRegime, setPointsRegime] = useState(false)` and set it in the existing zoomend handler (lines 813-818): `setPointsRegime(map.getZoom() >= TILE_AGGREGATION_MAX_ZOOM + 1)`; also set it once after `setReady(true)` (line 753) so a deep-linked `?z=15` load is right on first paint. Use `zoom` rather than `zoomend` if the legend should track a continuous wheel zoom.\n2. In the legend block (lines 1030-1112), when `pointsRegime` is true render a third branch regardless of `colour`: heading `t(\"map.reportType\")`, one swatch per CATEGORY_KEYS using CATEGORIES[k].color and `t(\\`categories.${k}\\`)` (labels already exist at apps/web/messages/en.json:24-29 and zh-TW.json), and a caption meaning \"one dot = one report\" (new key `map.perReport` in en.json and zh-TW.json). Drop the density ranges and the \"reports per cell\" caption in that state. Optionally disable or dim MapColourToggle while `pointsRegime` is true, since it has no effect on what is drawn.\n3. Fix the stale comments that say the handoff is z10: HeatmapView.tsx:735-737 and packages/shared/src/index.ts:308-314 (the doc comment above `TILE_AGGREGATION_MAX_ZOOM = 13` still argues for 10).\n4. Optional, longer-term: stop sharing hex values between CATEGORIES (#10b981, #f59e0b) and DENSITY_CLASSES so the z14–14.2 crossfade, where both regimes are briefly visible, cannot be misread either."
  },
  {
   "id": "directory-cap",
   "area": "discovery",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "CAP: apps/web/app/[locale]/(site)/species/(directory)/page.tsx:51-55 calls `listSpecies({ q, filter, limit: 80 })` — the limit is a hard-coded literal and no `offset` is passed. The searchParams type at page.tsx:35 is `{ q?: string; filter?: string }`, so a `page` param is never read. `listSpecies` does support paging (apps/web/lib/species.ts:142-143 `limit?`/`offset?`, :149-150 defaults 60/0, :209 `limit ${limit} offset ${offset}`) — the page just never uses it. Rendered output (page.tsx:97-132) is a `<ul>` followed directly by `</main>`: no next/more control, no total, no \"showing N of M\".\n\nLive check (curl localhost:3000): `/species`, `/species?filter=all`, `?filter=protected`, `?filter=endemic`, `?filter=invasive` and `/en/species` each render exactly 80 unique `href=\"/species/<id>-…\"` links; the HTML for each ends `</ul></main>`. So the cap binds on every filter, including the default. The code's own comments put the default set at 354 recorded species (page.tsx:43-44, species.ts:127-128), so 274 recorded species are unreachable by browsing — only search finds them; under `all` (~66k taxa) 80 are shown. The \"pagination\"/\"下一頁\" strings that do appear in the served HTML are the serialised next-intl bundle for the reports list (zh-TW.json:349-351, used only by reports/(list)/page.tsx:322), not a rendered control on this page. Note page.tsx:94-96 comment: \"80 results meant six screens of scrolling\" — the cap is deliberate, but there is no user-facing signal of it.\n\nEMPTY STATE: page.tsx:89-92 renders a single `t(\"noMatches\")` (\"找不到符合的物種。\" zh-TW.json:184 / \"No matching species.\" en.json:184) whenever `species.length === 0`, regardless of filter or query. Because the default filter is `recorded` (page.tsx:45-49), a checklist species with zero records disappears with the same message as a nonexistent one. Demonstrated by curl: `/species?q=Ursus` → 0 links + rendered no-match `<p>`; `/species?q=Ursus&filter=all` → 5 links. Same for `Formosania` (0 vs 1) and `Aphyocypris` (0 vs 2). `/species?q=zzzzqqqxx` and `?filter=all&q=zzzzqqqxx` render the identical `<p class=\"mt-8 text-center …\">找不到符合的物種。</p>`. Partial mitigation: the lede `directoryHint` (zh-TW.json:181 \"已有通報紀錄的物種…\") does say the default list is records-only, and the 「全部」 chip is one click away — but the empty-state text itself is literally false for a real species and offers no link onward.\n\nSeparate: apps/web/app/api/species/search/route.ts:36 caps autocomplete at 12 — that is a different surface (picker/map), not the directory, and is not what the review is describing.",
   "fix": "1) Pagination — copy the pattern already in apps/web/app/[locale]/(site)/reports/(list)/page.tsx:21,85-86,105-109 and 321-340. In species/(directory)/page.tsx: add `page?: string` to the searchParams type (:35); `const PAGE_SIZE = 80; const page = Math.max(1, Number(rawPage) || 1);` call `listSpecies({ q, filter, limit: PAGE_SIZE + 1, offset: (page-1)*PAGE_SIZE })` (:51-55); `hasNext = rows.length > PAGE_SIZE`, `species = rows.slice(0, PAGE_SIZE)`; after the `</ul>` (:132) render the same prev/next `<nav aria-label={t(\"pagination\")}>` with a `qs(page)` helper that preserves `q` and `filter`. Add `pagination`, `previous`, `next`, `pageN` keys to the `species` namespace in apps/web/messages/en.json and zh-TW.json (they exist only under `reports` today, zh-TW.json:349-351). Optional: a `countSpecies` in lib/species.ts sharing the WHERE clause (species.ts:161-179) to show \"第 1 頁 · 共 354 種\"; without it prev/next alone already signals more exist.\n\n2) Empty state — in page.tsx:89-92 branch: when `species.length === 0 && filter !== \"all\" && q`, run `listSpecies({ q, filter: \"all\", limit: 1 })`; if it returns a row, render a distinct message (\"名錄中有此物種，但尚無通報紀錄\" / \"This species is in the checklist but has no recorded observations\") with a `<Link href={{pathname:\"/species\", query:{q, filter:\"all\"}}}>` to view it; otherwise keep `noMatches`. Add `noMatchesUnrecorded` (and a link label) to both message files. Cheaper variant with no extra query: always append a \"search all species\" link to the empty state when filter is not `all`."
  },
  {
   "id": "list-row-target",
   "area": "discovery",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "Both halves of the claim are true; the review's reasoning is accurate.\n\nONLY LINK TO A RECORD IS THE DATE — apps/web/app/[locale]/(site)/reports/(list)/page.tsx\n- 263-272: the date cell is the one `<Link href={`/reports/${r.id}`}>`.\n- 273: category cell is plain text.\n- 274-287: species cell links to `/species/<slug>`, not the record.\n- 288-302: location cell is plain text.\n- 247-262: the (currently never-shown) photo thumbnail is a bare `<img>`, not a link. No row-level handler or overlay anchor exists.\n- curl http://localhost:3000/en/reports → 200; exactly 50 `href=\"/en/reports/<uuid>\"` on a 50-row page (one per row); the first `<tr>` contains exactly two anchors: one to /en/reports/<uuid> (date) and one to /en/species/37643-rattus-tanezumi.\n- Affordance: the date link is `className=\"text-ink-600 hover:text-ink-800\"` (266) with no underline. apps/web/app/globals.css:103-111 sets `body { color: var(--color-ink-600) }` and grep found no base `a` rule in globals.css. What the code does: the record link is rendered in the same colour as body text with no underline; I infer (not rendered) it is indistinguishable from the neighbouring cells until hovered.\n\nSPECIES/DATE FILTERS APPLIED BUT NOT DISPLAYED — same file\n- 68-72: `mapFilterSchema.safeParse(sp)` yields taxonId/from/to (schema at packages/shared/src/index.ts:182-193).\n- 100-103: all three are applied in the SQL where-clause.\n- 158-169: `activeFilter`/`withFilter` carry them into paging and category-chip hrefs.\n- 175-215: the only filter UI is the category-group chip nav. `aria-current=\"page\"` and the dark chip style are driven solely by `group`; taxonId/from/to are never rendered anywhere in the JSX. The `list` namespace in apps/web/messages/{en,zh-TW}.json has no strings for a filter summary or clear action.\n- curl ...?taxonId=32116 → 200, 22 rows, every species link is /en/species/32116-prionailurus-bengalensis (filter applied). Rendered text from the category nav to the table head: \"All Invasive species Wildlife sighting Roadkill or injured Reports ordered by date\"; the \"All\" chip carries aria-current=\"page\" with href=\"/en/reports?taxonId=32116\". No species name appears in rendered markup (the string hits for \"Prionailurus\"/\"石虎\"/\"Clear filters\" are inside the RSC flight payload's messages bundle for the map, e.g. `\"speciesSearch\":\"Search by name — 石虎, leopard cat, Prionailurus\"`, not the page).\n- curl ...?from=2017-01-01&to=2017-06-30 → 200, 50 rows, every date cell is 6/30/2017 (applied), next-page href keeps the range; same nav text, \"All\" still aria-current, no dates shown.\n- curl ...?taxonId=32116&from=2020-01-01&to=2023-12-31 → 200, zero rows; rendered main text is just \"No reports yet. Page 1\" — a visitor is not told why.\n- The handoff that makes this reachable: apps/web/components/map/HeatmapView.tsx:969-983 \"View as list\" skip link passes `filterToQuery(filter)` (group+taxonId+from+to) to /reports.\n\nNuance: the category `group` filter IS displayed (the chips). The review correctly limits the claim to species/date, so no correction is needed.",
   "fix": "1. Show active species/date filters in apps/web/app/[locale]/(site)/reports/(list)/page.tsx. After line 72, when `taxonId` is set fetch the name (reuse `getSpecies(id)` from apps/web/lib/species.ts:108, or a one-column select of scientific_name/common_name_zh from `taxa`, mirroring the join at 96-99). Under the chip nav (after 215) render a summary row: species name (Link to `/species/${speciesSlug(...)}`), the from/to range, and a \"clear\" Link built from `withFilter({})` with taxonId/from/to removed (keep `group`). Add `list.filteredBySpecies`, `list.filteredByDates`, `list.clearFilters` to apps/web/messages/en.json and zh-TW.json. Fold the same summary into the `<caption>` (222) and the empty-state message (218) so a screen-reader user hears what the list is filtered to, and consider not marking \"All\" `aria-current` (185) while a species/date filter is active — it currently announces \"All, current page\" on a species-filtered list.\n\n2. Make the row target discoverable. Minimal: change the date Link (266) to an underlined, darker style (e.g. `text-ink-800 underline decoration-ink-900/30 underline-offset-2 hover:decoration-ink-900`) and/or add a final `<th>`/`<td>` with an explicit \"Open\" Link to `/reports/${r.id}`. Larger: give the `<tr>` `relative` and the date Link a `before:absolute before:inset-0` stretched pseudo-element so the whole row is clickable, with the species Link `relative z-10` so it stays independently clickable. Update apps/web/app/[locale]/(site)/reports/(list)/loading.tsx only if a column is added."
  },
  {
   "id": "detail-photo-grid",
   "area": "discovery",
   "verdict": "confirmed",
   "severity": "low",
   "effort": "hours",
   "evidence": "apps/web/app/[locale]/(site)/reports/[id]/page.tsx:138-150 — the photo block is `{urls.length > 0 && (<div className=\"mt-4 grid grid-cols-2 gap-2\">{urls.map(u => <img ... className=\"w-full rounded-lg object-cover\" />)}</div>)}`. The grid is two columns regardless of `urls.length`, so one photo fills one cell. The page is `max-w-xl px-6` (line 114); with Tailwind v4 defaults and no `--container-*` override in apps/web/app/globals.css, that cell is roughly (576−48−8)/2 ≈ 260px on desktop and ≈167px on a 390px phone (inferred from the classes, not measured). Each `<img>` (lines 142-147) is bare: no wrapping `<a>`, no onClick, no `<dialog>`; it is a server component with no client gallery. A grep of apps/web/app, components and lib for lightbox|<dialog|role=\"dialog\"|zoom|enlarge|fullscreen hits only MapLibre zoom in map files (ReportMap, SpeciesMap, HeatmapView, LocationPicker, lib/map.ts, tiles route). There is no enlargement path anywhere. Two secondary observations: `object-cover` on line 146 has no effect because no height/aspect-ratio is set, so mixed-ratio photos would give uneven rows (inferred from CSS, not rendered); `alt=\"\"` on line 145 marks every report photo decorative. Photos per report cap at MAX_PHOTOS = 4 (packages/shared/src/index.ts:211); the form uses `capture=\"environment\"` with `multiple` (apps/web/components/report/ReportForm.tsx:373-375), so a single phone photo is the realistic common case. Reach today: `report_photos` holds zero rows (apps/web/app/[locale]/page.tsx:108-109 comment; the list page says the same at reports/(list)/page.tsx:143-150). curl http://localhost:3000/reports/c426ae34-16f0-4799-8255-5d6413ce3022 → 200, zero occurrences of `grid-cols-2`, and the only `<img>` tags are the 32px brand badge from the header. So no visitor can currently see the grid at all; the defect is latent until the first user report with a photo is filed.",
   "fix": "In apps/web/app/[locale]/(site)/reports/[id]/page.tsx lines 138-150: (1) make the layout depend on count — e.g. `className={urls.length === 1 ? \"mt-4\" : \"mt-4 grid grid-cols-2 gap-2\"}`, or keep the grid and give the first image `col-span-2` when the count is odd, so a lone photo spans the full content width; (2) add an explicit box (`aspect-[4/3]` or a fixed `h-*`) so `object-cover` actually crops, or drop `object-cover`; (3) provide enlargement — cheapest is wrapping each `<img>` in `<a href={u} target=\"_blank\" rel=\"noreferrer\">` (the signed URL from line 79 is the full-size original; it expires after 900s, which is fine for a click-through), or extract a small client `PhotoGallery` component using a native `<dialog>` + `showModal()` so page.tsx stays a server component; add an aria-label message key (e.g. `detail.enlargePhoto`) to both next-intl message files. Optionally give the images meaningful alt text (species/category + date) instead of `alt=\"\"`. The list page's thumbnail column (reports/(list)/page.tsx:249-254) is unaffected."
  },
  {
   "id": "gbif-copy",
   "area": "trust",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "TRUTH TODAY: nothing is published or registered with GBIF; publication is planned only.\n- scripts/export-dwca.ts:355-361 writes occurrence.txt / meta.xml / eml.xml to a local dir and stops; the file has no HTTP, IPT or registry client. Lines 28-29 tell the operator to zip by hand \"when uploading to an IPT\". DATASET.id at 45-46 is commented \"Placeholder until the project registers a DOI/UUID with GBIF\"; homepage is \"https://example.org\" (59); contactEmail is \"\" (60).\n- Repo-wide grep for ipt / registry.gbif / publisher account / datasetKey hits only the import direction (scripts/import-gbif.ts:65-94, 331, 341; scripts/build-evalset.ts:79). No env var names in .env/.env.example/.env.local/apps/web/.env mention GBIF/IPT/DWCA (names only checked, values not printed).\n- vercel.json crons: only /api/jobs/classify and /api/jobs/cleanup-orphans; no export job. apps/web/test/export.test.mjs:27-83 only pins the row filter (source='user', suppressed excluded), not any upload.\n- data/export/ on disk contains only production.sql (Aug 5); data/export/dwca does not exist, so the export has not even been run and kept locally.\n- docs/remaining-work.md:64-70: exporter built, \"currently emits zero rows ... What remains is the non-code part: fill in the real DATASET contact/homepage, register with an IPT or GBIF publisher account, and upload.\" Same in docs/launch-checklist.md:302-306 and docs/plan-heatmap-and-launch.md:177-182; docs/plan-heatmap-and-launch.md:153-154 lists \"uploading to GBIF\" as something not done without the owner. README.md:663-683 describes only the exporter; the only GBIF dataset URL in README (line 687) is TaiRON's, not ours. Per project memory and the docs, every record in the DB is imported TaiRON data, so a registered dataset would currently be empty anyway.\n\nSTRINGS SAYING RECORDS ARE PUBLISHED (present tense) — all introduced in d0f49ca 2026-09-13 \"Rebuild the front page\":\n- home.whatBody — apps/web/messages/en.json:436 \"every record is public, and they are published back to GBIF as a Darwin Core Archive\"; zh-TW.json:436 \"所有紀錄公開，並以 Darwin Core 標準回饋給 GBIF\". Rendered by apps/web/app/[locale]/page.tsx:331.\n- home.taironBody (home.* block; not listed by the review) — en \"it shows those records, and publishes new reports back as open data\"; zh \"也把新的通報整理成公開資料回饋出去\". Rendered by page.tsx:333.\n- Borderline: season.noRewardBody (d1da0e6 2026-09-09) \"These records are published as open data under this project's name\" / \"會以本計畫的名義作為開放資料發布\" — true of the site itself but reads like a GBIF dataset.\n\nSTRINGS SAYING PUBLICATION IS PLANNED / CONDITIONAL:\n- attribution.ourDataBody — en.json:303 \"We intend to publish them back to GBIF as a Darwin Core Archive\"; zh-TW.json:303 \"我們計畫依照 Darwin Core 標準將這些紀錄回饋給 GBIF\". Rendered by apps/web/app/[locale]/(site)/attribution/page.tsx:162. From initial commit c5647de 2026-08-05.\n- home.openBody — en.json:423 \"We plan to contribute our own records back to GBIF as a Darwin Core Archive\"; zh-TW.json:423 \"我們也計畫依 Darwin Core 標準把自己的紀錄回饋給 GBIF\". Rendered by page.tsx:461 — the SAME home page as whatBody. From eae507d 2026-08-06.\n- me.journeyHint — en.json:479 \"qualifies for the next Darwin Core export to GBIF\"; zh-TW.json:479 \"符合下一次 Darwin Core 匯出至 GBIF 的條件\". Rendered by (site)/me/page.tsx:258 only for a signed-in user with reports; the code comment at me/page.tsx:269-274 explicitly says \"Nothing here claims an upload that has not happened\". From d1da0e6 2026-09-09.\n\nNEUTRAL (about the import/seed direction, no conflict): attribution.gbifBody (en/zh:290), about.privacyBody (310), home.provenanceVia (428). about.whatBody does not mention GBIF.\n\nLIVE CHECK (curl localhost:3000, all 200): on / and /en the raw HTML contains whatBody, taironBody and openBody 3x each (DOM + RSC payload + message bundle) — i.e. actually rendered — while ourDataBody and journeyHint appear 1x (bundle only). On /attribution and /en/attribution ourDataBody is 3x. So a visitor reading the home page sees \"they are published back to GBIF\" in the \"What is this\" block and \"We plan to contribute our own records back to GBIF\" in the \"Open data\" block further down, in both locales.\n\nThe review's claim and its stated reason are both correct; it just under-counts the present-tense side by one string (home.taironBody).",
   "fix": "Bring the two Sep-13 strings into line with the older, accurate \"planned\" wording — do not flip the planned strings to present tense, because publication genuinely has not happened.\n1. apps/web/messages/en.json:436 home.whatBody → \"...every record is public, and we will publish them back to GBIF as a Darwin Core Archive.\" apps/web/messages/zh-TW.json:436 → \"所有紀錄公開，並將以 Darwin Core 標準回饋給 GBIF。\"\n2. home.taironBody in both files → en \"...it shows those records, and will publish new reports back as open data.\"; zh \"...也會把新的通報整理成公開資料回饋出去。\"\n3. Optional: season.noRewardBody → \"will be published as open data\" / \"將以本計畫的名義作為開放資料發布\".\n4. Optional guard: a small test in apps/web/test that fails if any message value matches /are published back to GBIF|回饋給 GBIF(?!的條件)/ without a future marker, until a real dataset exists.\nIf the owner would rather make the present-tense copy TRUE instead: fill DATASET.homepage/contactEmail in scripts/export-dwca.ts:59-60, register a GBIF publisher account or IPT, replace the placeholder id at line 46 with the issued dataset UUID, run/zip/upload the archive (docs/remaining-work.md:68-70), then update all five strings to link the live dataset — but note there are currently zero source='user' rows to publish, so that path only becomes meaningful once real submissions exist."
  },
  {
   "id": "moderation-ux",
   "area": "operations",
   "verdict": "confirmed",
   "severity": "low",
   "effort": "hours",
   "evidence": "Both halves of the claim are literally what the code does.\n\nTHUMBNAILS — apps/web/components/admin/ModerationRow.tsx:87-99. Each photo is rendered as `<img src={u} alt=\"\" className=\"h-24 w-24 rounded-lg object-cover\" />` (lines 91-96). Tailwind is v4.3.3 (apps/web/package.json:46) and apps/web/app/globals.css does not override `--spacing`, so h-24/w-24 is 96x96px; `object-cover` centre-crops anything non-square. There is no wrapping `<a>`, no onClick, no lightbox — the only way to see the whole photo is to open the image URL by hand. Meanwhile the URL is the full stored object: admin/page.tsx:80-89 calls `signedPhotoUrl(p, 900)`, which is a plain `createSignedUrl` with no transform (apps/web/lib/supabase/service.ts:53-58), of a file the uploader downscaled to a 2048px max edge WebP (apps/web/lib/image.ts:44-57, packages/shared/src/index.ts:222). So the browser downloads a ~2048px image per photo, for up to 100 pending rows (page.tsx:78), and shows 96px of it. For contrast, the public detail page shows the same photos `w-full` in a 2-column grid (apps/web/app/[locale]/(site)/reports/[id]/page.tsx:138-149) — the public sees a report's evidence larger than the moderator who decides whether to publish it.\n\nPROMPT — ModerationRow.tsx:112-122, specifically lines 116-117: `const reason = prompt(t(\"rejectReason\")) ?? \"\"; if (reason) act(() => rejectReport(props.id, reason), \"rejected\");`. That is `window.prompt`: single-line, unstyled, browser-chrome OK/Cancel buttons that ignore the site locale. Cancel or an empty string silently does nothing — no validation message, no state change. It is the only `prompt(`/`confirm(` call in apps/web. The strings exist (apps/web/messages/zh-TW.json:169 \"退回原因？\", en.json:169 \"Reason for rejection?\"). The server side is fine: actions.ts:37-45 `rejectReport` stores the reason in `moderation_actions.reason` inside a transaction after `requireModerator()`; the problem is purely the client UI.\n\nCONTEXT — No `<dialog>`, `showModal`, `role=\"dialog\"` or lightbox component exists anywhere under apps/web/components or apps/web/app (grep empty), so there is nothing to reuse. No test under apps/web/e2e or apps/web/test references `/admin`, `ModerationRow`, `rejectReport` or a Playwright `dialog` handler, so nothing breaks on a fix — and nothing covers this UI today. Related: `setReportTaxon` (actions.ts:52-62) is exported but not wired to any control in ModerationRow, so moderators cannot correct species from this screen.\n\nHTTP — `curl -s -o /dev/null -w '%{http_code}' localhost:3000/admin` → 200 with \"請先登入。\"; `/en/admin` → 200 with \"Sign in\". The route is live, but rows only render for role moderator/admin (page.tsx:35-65), so the img/prompt facts above come from reading the code, not from an observed response.",
   "fix": "All in apps/web/components/admin/ModerationRow.tsx plus two message files; no server change required.\n\n1. Photos: make each thumbnail open the full image. Minimal: wrap the `<img>` (lines 91-96) in `<a href={u} target=\"_blank\" rel=\"noreferrer\">` and enlarge to e.g. `h-40 w-40 object-contain bg-ink-900/5` so nothing is cropped. Better: add a small `<dialog>` lightbox in the row (useRef + showModal) that shows the image at `max-h-[80vh] max-w-[90vw] object-contain` with a close button; there is no existing dialog component to reuse, so it is new code (~40 lines). Optional bandwidth win: in admin/page.tsx:85 request a Supabase transform for the thumbnail (`createSignedUrl(path, exp, { transform: { width: 400 } })` via a new option on signedPhotoUrl in lib/supabase/service.ts:53) and keep the untransformed URL for the lightbox — verify the project's Supabase plan supports image transforms before relying on it.\n\n2. Reject reason: replace lines 115-118 with inline state — `const [rejecting, setRejecting] = useState(false); const [reason, setReason] = useState(\"\")`. Clicking Reject sets `rejecting`, which renders a `<textarea>` labelled with the existing `t(\"rejectReason\")` plus Confirm/Cancel buttons under the action row; Confirm calls `act(() => rejectReport(props.id, reason.trim()), \"rejected\")` and shows a validation line when the trimmed reason is empty instead of silently doing nothing. Add strings `admin.rejectConfirm`, `admin.rejectCancel`, `admin.rejectReasonRequired` to apps/web/messages/zh-TW.json and apps/web/messages/en.json (the `admin` block is around lines 160-170 in both).\n\n3. Optional: add an e2e test under apps/web/e2e that signs in as a seeded moderator and exercises publish/reject, since nothing covers this screen."
  },
  {
   "id": "receipts-indistinct",
   "area": "operations",
   "verdict": "confirmed",
   "severity": "low",
   "effort": "hours",
   "evidence": "What the code does (read, not inferred):\n\napps/web/components/report/QueueBanner.tsx:148-170 — the receipts block. Lines 155-167 map every `sent` row to `<li key={r.id}><Link href={`/reports/${r.reportId}`}>{t(\"viewSent\")}</Link></li>`. The only per-item variation is the React `key` and the `href`; the visible text is the same translated string for every receipt. No timestamp, category, species or coordinate is rendered.\n\napps/web/messages/en.json:275 `\"viewSent\": \"Open it\"`; apps/web/messages/zh-TW.json:275 `\"viewSent\": \"開啟\"`. So in English the banner reads \"Sent N reports.\" (en.json:272) followed by N links all labelled \"Open it\"; in zh-TW, N links all labelled \"開啟\".\n\nQueueBanner.tsx:48 `RECEIPT_TTL_MS = 24h` and :60 `purgeUploaded(RECEIPT_TTL_MS)` — receipts accumulate for a day, so two or more identical links is a real, intended state, not an edge case.\n\nContrast within the same file: the still-pending list at QueueBanner.tsx:217-241 does render `new Date(i.createdAt).toLocaleString()` per row (line 224). Only the sent-receipt list lacks any distinguishing text. Wording note: the review calls these \"queued-report receipts\"; strictly they are receipts for reports that were queued and have since been sent (status \"uploaded\", queue.ts:66). The reason the review gives — nothing tells them apart — is correct.\n\nThe data to tell them apart is already on each receipt row: apps/web/lib/offline/queue.ts:131-143 `markUploaded` writes `status: \"uploaded\"`, `reportId`, `sentAt`, and clears only `photos` and `lastError`; `createdAt` (queue.ts:30) and the full `payload` (category, lng, lat, observedAt, taxonId, notes — queue.ts:31-53) survive. queue.ts:129's comment (\"The row that remains is an id, a timestamp and a link\") undersells it: the payload remains too.\n\n`reportId` is reliably present: apps/web/lib/offline/flush.ts:123 `markUploaded(item.id, String(data.id ?? \"\"))`, and apps/web/app/api/reports/route.ts:158-165 always returns `id` on 200/201. So the `r.reportId &&` guard at QueueBanner.tsx:157 is defensive and links are not normally dropped.\n\nMount point: apps/web/app/[locale]/(site)/report/page.tsx:41 `<QueueBanner />`.\n\nHTTP: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/report` → 200; `/en/report` → 200. The SSR HTML contains \"Open it\" and \"Sent {count} reports.\" only inside the embedded next-intl message bundle; the receipts themselves render client-side from IndexedDB, so curl cannot exercise the banner state. The rendered appearance (N identical links) is inferred from the JSX, not observed in a browser.\n\nAdjacent nit seen while checking: en.json:272 `\"sentCount\": \"Sent {count} reports.\"` has no ICU plural, so a single receipt reads \"Sent 1 reports.\" (en.json:259 `cardRecordCount` shows the plural pattern the file already uses). zh-TW is fine.",
   "fix": "In apps/web/components/report/QueueBanner.tsx lines 154-168, give each receipt its own label from data already on the row (no storage or schema change needed, since markUploaded keeps payload/createdAt/sentAt):\n\n- Render `new Date(r.payload.observedAt ?? r.createdAt).toLocaleString()` and the category label per receipt, mirroring the pending rows at lines 218-239 (which already show `toLocaleString()`), and use the `flex items-center justify-between` row layout so the two lists look like one system.\n- Category label: the `categories` namespace exists (en.json:24) and is used as `t(`categories.${category}`)` in apps/web/components/map/ReportPanel.tsx:96 and apps/web/app/[locale]/(site)/reports/[id]/page.tsx:128. QueueBanner's `t` is scoped to \"offline\" (line 37), so add `const tc = useTranslations(\"categories\")` and call `tc(r.payload.category)`.\n- Either make that text the link's content, or keep \"Open it\"/\"開啟\" as the link and add an `aria-label` that includes the date and category, so screen-reader link lists are not N identical entries.\n- Optionally show `sentAt` (\"sent 14:02\") as secondary text so a reporter can match receipts to the moments they watched them go.\n- While there: change en.json:272 to `\"sentCount\": \"{count, plural, one {Sent # report.} other {Sent # reports.}}\"`.\n\nNo change needed in lib/offline/queue.ts or flush.ts."
  },
  {
   "id": "error-links",
   "area": "operations",
   "verdict": "partly",
   "severity": "low",
   "effort": "hours",
   "evidence": "LINK LABEL — true for 2 of the 4 error pages, not all.\n- apps/web/app/[locale]/error.tsx:34-39: <Link href=\"/\">{t(\"backHome\")}</Link>\n- apps/web/app/[locale]/not-found.tsx:12-14: <Link href=\"/\">{t(\"backHome\")}</Link>\n- apps/web/messages/en.json:281 \"backHome\": \"Back to map\"; messages/zh-TW.json:281 \"backHome\": \"回到地圖\". Key name is backHome, so the key was right and the string wrong.\n- \"/\" is the landing page, not the map: app/[locale]/page.tsx:111-131 renders HomePage (badge, doors, ledger); the map is app/[locale]/map/page.tsx. i18n/navigation.ts:9-10 Link just prefixes the locale, so href=\"/\" -> \"/\" or \"/en\".\n- curl: GET / -> 200, GET /en -> 200 (no redirect). GET /en/species/does-not-exist-xyz -> 404, body contains \"Back to map\" x3 and \"Browse species\" x2. GET /species/does-not-exist-xyz -> 404, body contains 回到地圖 x3.\n- NOT true for app/global-not-found.tsx:43-54 (the top-level 404 for URLs matching no route, which its own comment at :14-21 calls \"most real 404s\"): 回首頁 -> href=\"/\" and 查看地圖 -> href=\"/map\", both correctly labelled. curl GET /zzz-nope -> 404 with exactly those two anchors. app/global-error.tsx has no link at all, only a Retry button (:15-20).\n- Why it is stale: at c5647de (2026-08-05, where the string was introduced) app/[locale]/page.tsx imported HeatmapView, i.e. \"/\" WAS the map, so \"Back to map\" was accurate then. /map was split out in eae507d (2026-08-06) and HeatmapView left the root page in e15e820 (2026-09-13); the string was never revisited. The site elsewhere uses a separate key nav.backToMap (\"← Back to map\", en.json:8) that does point at /map (app/[locale]/(site)/reports/[id]/page.tsx:115-120).\n\nGLOBAL-ERROR COLOURS — true, with a nuance the review missed: the page is half-migrated, not wholly old.\n- app/global-error.tsx:13 color \"#94a3b8\" (Tailwind slate-400), :14 \"#475569\" (slate-600), :17 button background \"#10b981\" (emerald-500). None of these hex values appear in the @theme block, app/globals.css:22-97. globals.css:7-8 names exactly this as the retired look: \"The previous UI was slate-blue with an emerald button\".\n- But :11 background \"#0b1410\" and color \"#ece2cd\", and :17 button text \"#0b1410\", ARE current tokens: --color-bark-950 (globals.css:51) and --color-parchment-100 (globals.css:61). git diff c5647de..eae507d on this file shows only those two values were swapped (#020617->#0b1410, #e2e8f0->#ece2cd); the muted text, digest and button fill were left on slate/emerald.\n- Secondary inconsistency: this page sits on the dark map surface (bark/parchment) while the other three error pages and every content page use the light paper/ink surface (globals.css:13-20, 106-110; error.tsx:20-36, not-found.tsx:7-15, global-not-found.tsx:33-51) with ember-500 (#cf7238, globals.css:85) as the action colour.\n- Could not observe global-error rendered: it only mounts for root-layout failures and only in production builds, so the colour finding is from code reading alone. The link finding is confirmed by both code and curl output.",
   "correction": "The mislabelled \"Back to map\" link exists only on the locale-level pages app/[locale]/error.tsx and app/[locale]/not-found.tsx. The top-level 404, app/global-not-found.tsx, which handles most real dead URLs, is correctly labelled (回首頁 -> \"/\", 查看地圖 -> \"/map\"), and app/global-error.tsx has no link at all. The label was accurate when written (the map was at \"/\" on 2026-08-05) and went stale when the map moved to /map; the i18n key is already named backHome. On global-error.tsx, the review's reason is only half right: its background and body text (#0b1410, #ece2cd) are current bark-950/parchment-100 tokens; only the muted text (#94a3b8), digest (#475569) and button fill (#10b981) are the retired slate/emerald palette. The page is also on the dark map surface while every other error page is on light paper, which the review did not mention.",
   "fix": "1. Fix the string, not the href (both pages already go to \"/\", and global-not-found.tsx already uses the right wording): apps/web/messages/en.json:281 \"backHome\": \"Back to home\"; apps/web/messages/zh-TW.json:281 \"backHome\": \"回首頁\". Optionally add a second \"/map\" link to app/[locale]/not-found.tsx mirroring app/global-not-found.tsx:49-54 (查看地圖 / \"View the map\"), reusing nav.backToMap or a new errors.viewMap key.\n2. app/global-error.tsx: keep inline hex (no stylesheet loads there, see its comment at :3-7) but use current tokens and comment their names so drift is visible. Either stay dark and match the map chrome: :13 #94a3b8 -> #9d9179 (parchment-400), :14 #475569 -> #8b8270 (parchment-500, the AA-checked muted step per globals.css:65-69), :17 #10b981 -> #cf7238 (ember-500) keeping text #0b1410; or, to match the other three error pages, go light: body #faf7f0 (paper-50) / #22362b (ink-800), muted #51695b (ink-500), button #cf7238 with #0b1410 text.\n3. If wanted, a one-line test that errors.backHome does not contain \"map\"/\"地圖\" would stop the label drifting again; today nothing under apps/ references the string except the two pages."
  },
  {
   "id": "header-badge",
   "area": "identity",
   "verdict": "confirmed",
   "severity": "low",
   "effort": "hours",
   "evidence": "CODE (verified): apps/web/components/brand/Wordmark.tsx:41 `const mark = size === \"lg\" ? 44 : size === \"md\" ? 32 : 28;` and :53 `<Badge size={mark} className=\"shrink-0\" />` — Wordmark always renders the full Badge, never a simplified mark. apps/web/components/brand/Badge.tsx:35-43 renders next/image of public/brand-badge.png (512x512 PNG with alpha, per `file`/`sips`) clipped rounded-full; its own docblock at :5-10 says the two rings of type and the pangolin's scales \"turn to mud below about 48px ... Badge at 64px and up, Mark below\" and still references a `Mark` component that does not exist. apps/web/components/site/SiteHeader.tsx:79 `<Wordmark size={app ? \"sm\" : \"md\"} />` — 28px on the /map app header, 32px on site/page headers. apps/web/components/site/SiteFooter.tsx:77 uses size=\"md\" (32px) as well. GIT: Mark.tsx (a 64-unit inline SVG: three rows of pointed scales, dashed road, two ember rivets; added eae507d, redrawn 68c5efd) was deleted in commit 5798555 \"One logo everywhere, including the browser tab\", which replaced `<Mark className=\"size-8\">` with `<Badge size={32}>`. That commit message itself says the badge's rings and scales \"stop resolving below about 48px, and that is still true\" and \"At 16px it is a green disc with the cream sky showing — recognisably the badge, not legible as it. That is the trade being made deliberately.\" HTTP (curl localhost:3000, all 200): `/` and `/about` header emit `<img width=\"32\" height=\"32\" srcSet=\".../brand-badge.png&w=32 1x, ...&w=64 2x\" src=\"...&w=64\">`; `/map` emits `width=\"28\" height=\"28\"` with the same 32/64 srcset. No `images` block in apps/web/next.config.ts, so next/image defaults apply; no `sizes` prop on the header badge. The only large renders are the homepage hero (app/[locale]/page.tsx:173-178, size=320) and /about (app/[locale]/(site)/about/page.tsx:54, size=112). INFERRED, NOT RENDERED: I did not screenshot (no headless browser per rules). \"Illegible\" follows from geometry — two concentric rings of lettering from a 512px source in 28-32 CSS px — and from the authors' own comments and commit message asserting exactly that; a 64px source at 2x means resampling quality is not the cause, the slot size is. NUANCE: the review's reason is correct, but this is a documented deliberate trade-off, not an oversight. Wordmark.tsx:27-28 also notes the artwork still letters the two-names-old 生態守望計畫 / PROJECT ECOWATCH, so the illegible ring is currently masking a stale name; a legible header mark built from the badge would expose that until the artwork is redrawn.",
   "fix": "Restore a simplified mark for header/footer sizes and keep the badge for 64px+. Concretely: (1) `git show 5798555^:apps/web/components/brand/Mark.tsx` recovers the abstract scales-over-road SVG (uses fill-bark-800, stroke-parchment-200, fill-scale-300/400/500, fill-ember-500 token classes, all still in apps/web/app/globals.css); re-add it as apps/web/components/brand/Mark.tsx. (2) In apps/web/components/brand/Wordmark.tsx:41-53 branch on size: render `<Mark className=\"size-7|size-8 shrink-0\" />` for \"sm\"/\"md\" and `<Badge size={44}>` (or larger) for \"lg\", and update the :37-40 comment. (3) Update the stale docblock in Badge.tsx:7-10 to match. (4) Optionally re-derive app/icon.png and app/apple-icon.png from Mark at 16-48px, since 5798555 also switched the favicon to the badge with the same legibility loss; leave the OG images on the badge. If the team wants to keep the \"one logo everywhere\" decision, the alternative fix is redrawing brand-badge.png so the ring lettering is the current name and the header stays as is — but that does not make 32px lettering legible, it only removes the stale-name problem."
  },
  {
   "id": "biowatch-stats",
   "area": "biowatch",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "hours",
   "evidence": "All five parts of the claim hold in the code (biowatch is not the app on localhost:3000, which is apps/web; verified by reading the server component, plus curl of the endpoint it depends on).\n\n1. FormosaWatch fallback figures. apps/biowatch/lib/projects.ts:31-45 `formosawatchCounts()` fetches `${FORMOSAWATCH}/api/health` with a 5 s timeout and returns null on !ok, non-numeric `reports`, or any throw. projects.ts:65 `value: formosa ? n(formosa.reports) : \"46,000+\"` and :69 `value: formosa && formosa.species ? n(formosa.species) : \"450+\"`. The hardcodes date from the first commit 1a9f108 (2026-08-11) and have not changed. The endpoint exists: apps/web/app/api/health/route.ts:14-22 selects reports/taxa/species; `curl localhost:3000/api/health` -> `{\"ok\":true,...,\"reports\":46334,\"taxa\":125438,\"species\":458,...}` HTTP 200. Production could not be reached from this machine (curl HTTP 000; all three biowatchintl.org names resolve to 10.1.1.226 locally), so whether prod health already returns `species` is unverified.\n\n2. FlamaWatch static figures. projects.ts:86-94: comment \"Static, unlike FormosaWatch's... these are the figures it states on its own home page\", values \"171 wildfires in 2026\", \"346 illegal burns in 2026\", \"150 ha lost 2001–2025\", `live: false` at :95. Copied 2026-08-11; today is 2026-09-17 and the \"in 2026\" labels carry no as-of date. apps/biowatch/README.md:50-53 lists this under Known gaps.\n\n3. No source/date label. apps/biowatch/components/Project.tsx:55-66 renders only `s.value` and `s.label` per stat; nothing on the card or page (app/page.tsx) prints a source, an \"as of\", or \"live/approximate\". grep for note/source/updated/as of across apps/biowatch finds no rendering.\n\n4. `live` ignored. Project.tsx:16 declares `live: boolean`; the component body :27-76 never reads `p.live`. `git log -p` shows it was added in 1a9f108 and never consumed. The doc comment at Project.tsx:14 promises a per-stat `note` field that the `stats` type (:15) does not have.\n\n5. \"Use the data\" -> email. page.tsx:203-207 `href: \\`mailto:${CONTACT}\\``, CONTACT defaulting to hello@biowatchintl.org (page.tsx:6-7); README.md:43 says that default is a placeholder \"Until the real address exists\" (production env unverified).\n\n6. Common-operator implication. page.tsx:85 \"Two places, one method\"; :88-90 \"Each project is run in its own language, for its own country... They share an approach, not a template\" (never says by whom); hero :59 \"BioWatch builds the tools\"; \"Report something\" card :197-200 \"Pick the project for where you are — Taiwan · Colombia\"; footer :251-255 lists both projects under the BIOWATCH lockup; layout.tsx:13 metadata \"currently in Taiwan and Colombia\"; the FlamaWatch link defaults to a biowatchintl.org subdomain (projects.ts:15-17). Against that, projects.ts:12-14 and README.md:14-17 say FlamaWatch is \"run by a separate organisation... nothing of it is built from this repository\" and its address is unconfirmed; docs/improvements.md:226-227 calls it \"someone else's remit\". Nothing on the page says so.",
   "correction": "The claim is true but two of its framings are narrower than the code. (a) The fallback is not only \"when its endpoint fails\": it also fires on a 5 s timeout, a non-numeric `reports`, and — for the species figure only — whenever `species` is 0 or absent (projects.ts:38, :42, :69), in which case the card shows a live records count next to a hardcoded \"450+\" while `live` is still true (:74). The \"2011–\" stat is static even on the live card. So simply rendering the existing card-level `live` flag would itself be inaccurate; provenance has to be per stat. (b) \"Use the data\" pointing at email is not just a wrong href: there is no data-access destination to swap in yet. apps/web exposes no public list/export API (api/reports/route.ts:21 is POST-only; [id]/route.ts:36 is single-record) and the GBIF publication is still future tense (apps/web/messages/en.json:303, :423 \"We intend to publish\"), so the page's own \"CC BY, published back to GBIF\" tile (page.tsx:168) overstates too. The nearest real targets today are FormosaWatch's /reports list and /attribution page. Also, a \"live\" figure can be up to an hour stale (page.tsx:4 and projects.ts:33 revalidate 3600) and is equally unlabelled.",
   "fix": "1. apps/biowatch/components/Project.tsx: replace `live: boolean` (:16) with per-stat provenance, e.g. `stats: { value; label; note?: string }[]` (the `note` the comment at :14 already describes), and render the note as small text under the `<dl>` (:55-66), e.g. \"Live from FormosaWatch, refreshed hourly\" / \"Approximate — FormosaWatch was unreachable when this page was built\" / \"From FlamaWatch's site, as of Aug 2026\". 2. apps/biowatch/lib/projects.ts: attach those notes at :63-73 and :90-94; date the FlamaWatch figures (copied 2026-08-11) and drop or date the \"in 2026\" year-to-date labels; make `formosawatchCounts` return which fields were live so the species fallback (:69) is labelled independently. 3. apps/biowatch/app/page.tsx:203-207: point \"Use the data\" at `${FORMOSAWATCH}/attribution` (or /reports) with the email as a secondary line; change the :168 tile \"published back to GBIF\" to \"to be published to GBIF\" until apps/web actually publishes. 4. page.tsx:85-91 and the FlamaWatch card body (projects.ts:85): add one sentence that FlamaWatch is run independently by its own organisation and BioWatch links to it; consider the footer (:251-255) grouping it under \"Partner project\". 5. Minor: README.md:42 documents NEXT_PUBLIC_FIREWATCH_URL but code reads NEXT_PUBLIC_FLAMAWATCH_URL (projects.ts:16); confirm the FlamaWatch address and the contact mailbox exist in production before relying on either."
  },
  {
   "id": "season-coverage",
   "area": "supporting",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "day",
   "evidence": "WHAT THE CODE DOES\n\n1. coverage() returns only scalars. apps/web/lib/coverage.ts:35-48 types the result as seven fields (covered, newThisSeason, unplaceable, speciesRecorded, speciesInChecklist, seasonStart, seasonEnd). The `cells` CTE at :62-84 does compute each cell's gx/gy on a floor(x/5000) lattice, but the outer select at :86-97 only counts them. No cell identity, geometry or list ever leaves the function.\n\n2. The season page consumes only those scalars. apps/web/app/[locale]/(site)/season/page.tsx:51-59 reads c.newThisSeason, c.covered, c.unplaceable, species counts and seasonEnd; renders a number (:71-77), an aria-hidden progress bar (:81-89), two Figure tiles (:108-117), prose, and three links: /report (:95-100), bare /map with no query string (:136-141), /stats (:142-147). Nothing on the page names, lists, or draws a square.\n\n3. HTTP check: `curl http://localhost:3000/en/season` -> 200. Rendered main text is \"0 / 40 new 5 km squares reached ... 1,366 5 km squares with a record ... 2,721 published records are left out ... Open the map / All statistics\". No coordinates, no list, no map.\n\n4. No other surface shows the coverage lattice. grep -il coverage across apps/web, packages, supabase hits only season/page.tsx, lib/coverage.ts, stats/page.tsx, messages/*.json, and tests. The stats page's \"coverage\" Section (stats/page.tsx:288-300) is about obscured records and links to /about. API routes (apps/web/app/api/*) are health, jobs, reports, species/search, tiles, uploads: none serves cells.\n\n5. The map cannot stand in for it. The tile route apps/web/app/api/tiles/[z]/[x]/[y]/route.ts:148-176 only emits cells that HAVE reports (group by over reports_public), never empty ones; its grid is st_snaptogrid (:151, nearest-centre lattice) at aggregationCellMeters(z) = 40075016.686/2^z/128 (packages/shared/src/index.ts:416-420), i.e. 4,892 m at z6, 2,446 m at z7, never 5,000 m and offset half a cell from coverage.ts's floor lattice. The tile query also has no `not is_obscured` filter (:159-163), whereas coverage.ts:82 excludes obscured rows, so a blurred sensitive record can make a square look occupied on the map while it still counts as uncovered for the season. HeatmapView.tsx layers (:204-210) are reports-cells/dots/heat/points only; grep for empty/uncovered/coverage in components/map finds nothing but comments about blank-map bugs.\n\n6. Hotspots (apps/web/lib/stats.ts:198-245) do share coverage.ts's floor(x/5000) lattice but list the 8 densest cells, the inverse of what the goal needs, linking to /map?lng&lat&z=11 (stats/page.tsx:243-252).\n\nWHAT THE REVIEW'S REASON GETS RIGHT/WRONG\nThe stated reason (\"nobody can act on it\") is right, and the page's own copy makes it sharper: page.tsx:33-36 and coverage.ts:13-17 say the only way to move the number is to visit somewhere nobody has been, and the rendered \"noneYet\" string tells visitors that a first record from a never-recorded square is the most useful thing to file. The page instructs the visitor to find an uncovered square and gives no means to. The one caveat the review misses: \"uncovered\" is not defined anywhere in the code either. coverage.ts has no denominator (no count of land cells), so rendering the empty squares needs either a land mask or the simpler inverse (draw the 1,366 covered squares over a basemap so blank land reads as uncovered).",
   "fix": "Minimal, end-to-end:\n\n1. apps/web/lib/coverage.ts: add `coveredCells(): Promise<{gx:number; gy:number}[]>` (or a GeoJSON FeatureCollection built in SQL with st_makeenvelope((gx)*5000,(gy)*5000,(gx+1)*5000,(gy+1)*5000,3857) transformed to 4326), reusing the same `where not is_obscured` + floor(x/5000) grouping as lines 62-84 so it is the exact set the counter counts. ~1,366 rows; cache like the page (revalidate 900).\n\n2. Serve it: either a new route apps/web/app/api/coverage/route.ts returning that GeoJSON with public cache headers, or pass it server-side as a prop from apps/web/app/[locale]/map/page.tsx when a `layer=coverage` query param is present (parse it next to parseView at :65-93; keep it out of mapFilterSchema so tile URLs stay canonical).\n\n3. apps/web/components/map/HeatmapView.tsx: add a `coverage-cells` geojson source + fill/line layer drawn under the report layers (see addReportLayers at :518-560), visible only when the coverage prop is set; a legend line \"shaded = already has a record; blank land = still needs one\". Optionally a second hatched layer for the TAIWAN_MAIN_BOUNDS lattice cells not in the set, if a land mask is available (skip otherwise).\n\n4. apps/web/app/[locale]/(site)/season/page.tsx:136-141: change the bare `/map` link to `{ pathname: \"/map\", query: { layer: \"coverage\" } }` and add en/zh-TW strings (messages/*.json `season.seeMap` or a new `season.seeUncovered`) saying it shows which squares are still empty. Same link could go on the \"noneYet\" paragraph.\n\n5. apps/web/test/coverage.test.mjs: add a test that inserting a report in an empty sea square adds exactly one cell to coveredCells() and that an obscured-taxon report adds none, mirroring the existing rollback tests."
  },
  {
   "id": "ledger-coords",
   "area": "homepage",
   "verdict": "confirmed",
   "severity": "medium",
   "effort": "day",
   "evidence": "Both halves of the claim are true in the code and in the rendered page.\n\nRAW COORDINATES PER ROW\n- apps/web/app/[locale]/page.tsx:380-408 renders each ledger row as date | species | `{r.lat.toFixed(4)}, {r.lng.toFixed(4)}` (lines 402-404). The coordinate span is `hidden ... sm:inline`, so it shows at >=640px and is absent on phones (no custom breakpoint in apps/web/app/globals.css; the @theme block at line 22 defines none).\n- apps/web/lib/stats.ts:149-185 `anniversaryLedger` selects only id, observedAt, st_y/st_x of location_public, commonNameZh, scientificName. No place field exists to render.\n- curl http://localhost:3000/en (and /): 10 rows, e.g. `2017-09-20 | Ixobrychus sinensis 黃小鷺 | 23.2922, 120.1448` and `2017-09-19 | Passer montanus 麻雀 | 24.3228, 120.5529`. zh-TW is the same with name order swapped.\n- There is no place-name data anywhere: the reports table (supabase/migrations/0001_init.sql:116-146) and the reports_public view (0010_capture_accuracy.sql:33-56) have no locality/county/township column; migrations 0001-0010 create no admin-boundary table (the only grep hits for county/admin are comments at 0001_init.sql:79, 0003_reporting.sql:33 and a MapLibre filter note at apps/web/lib/basemap.ts:80). The importer's Occurrence type (scripts/import-gbif.ts:29-42) reads only coordinates, date, names, licence, rightsHolder, datasetKey; it does not ingest GBIF's stateProvince/county/municipality/locality.\n- This is the site-wide convention, not a ledger quirk: reports/[id]/page.tsx:209 prints `{row.lat?.toFixed(4)}, {row.lng?.toFixed(4)}` and reports/(list)/page.tsx:289 prints `toFixed(3)` coordinates. The list page's own comment (lines 114-116) concedes \"a table of dates and coordinates ... reads as a database export\".\n\nBLANK ROW DOES NOT SAY FIRST OF WHAT\n- page.tsx:369-378: `<Link href=\"/report\">` with a dash placeholder `————-——-——` and `{t(\"ledgerBlank\")} →`.\n- apps/web/messages/en.json:443 `\"ledgerBlank\": \"File the first one\"`; zh-TW.json:443 `\"通報第一筆\"`. Rendered: `File the first one →` / `通報第一筆 →`.\n- The only context is the section title (en.json:441 \"This week, in other years\") and hint (en.json:442 \"... across 2011-2017. The first line is blank.\"). Neither says the blank line is this year's / 2026's entry for this week; the row itself carries no year, no date, no object. With the ledger's rows ending at 2017, \"first\" is genuinely ambiguous between \"first since 2017\", \"first this year\", and \"first ever by you\".\n\nNUANCE ON THE REVIEW'S FRAMING\n- On phones the row shows no location at all, so \"raw lat/lng per row\" applies to tablet/desktop only.\n- The review implies a display choice; it is actually a data gap. There is no place name to show without new data, and the same gap affects the report list and detail pages.\n- The blank row is at least partly explained by the hint one paragraph above (\"The first line is blank\"), but that sentence explains that it is blank, not what filing would make it.",
   "fix": "1. Place names (data + query + render):\n   - Add supabase/migrations/0011_admin_areas.sql creating a `taiwan_admin` table (county + township polygons, zh and en names; MOI open data or OSM) with a GiST index, and a loader script alongside scripts/import-gbif.ts.\n   - In apps/web/lib/stats.ts `anniversaryLedger` (149-185), add a lateral `st_within(r.location_public::geometry, a.geom)` join returning `countyZh/countyEn/townshipZh/townshipEn`, and extend `LedgerRow`.\n   - In apps/web/app/[locale]/page.tsx:402-404 replace the coordinate text with the locale-appropriate \"township, county\" label (keep coordinates as `title=` if wanted). Privacy is unchanged: the ledger already excludes obscured rows (stats.ts:165) and location_public is the already-blurred point, so a township label reveals no more than the coordinate it replaces.\n   - Optionally reuse the same join in reports/(list)/page.tsx:91-93,289 and reports/[id]/page.tsx:64-65,209 so the convention changes everywhere at once.\n   (Cheaper but incomplete alternative: ingest GBIF stateProvince/county/municipality in scripts/import-gbif.ts into new nullable columns; this needs a re-import of ~46k rows and gives nothing for user-submitted reports, which only carry a dropped pin.)\n\n2. Blank row (copy only):\n   - Change `home.ledgerBlank` in apps/web/messages/en.json:443 and zh-TW.json:443 to name the object, e.g. \"File this week's first record for {year}\" / \"通報 {year} 年這一週的第一筆\", passing `{ year }` at page.tsx:376.\n   - Optionally replace the dash placeholder at page.tsx:373 with today's date in Asia/Taipei formatted like the other rows, so the row's geometry itself says \"this year's line\"."
  }
 ],
 "refuted": [],
 "unchecked": []
}