# W4 — Home

Paths relative to `apps/web` (except `scripts/`), read at c73bb8d. Read first: `../direction.md` §2.5, §2.6, §4 "Home"; `../report-flow.md` "Decision"; `../inventory/home.md`; `shots/home*-fold.png`.

**Goal**
A visitor to `/` or `/en` sees in one viewport: the emblem very large, the name, one line, ONE report action, one way to the map, and whose records these are. Below: routes into the map, real records from this week in other years, one credit sentence, footer. No question, no category doors, no count, no picture of the data, no coordinates.

**Why / evidence**
- The owner rejected 你看到了什麼？ and choosing a category first. It is this page's main section: `DOORS`, `app/[locale]/page.tsx:70-74, 219-258`, under the heading at `:221`.
- "Logo a lot bigger, only a little text": the emblem is 320px desktop, 158-172px on a phone under ~150px of chrome (`:173-178`, `shots/home-phone-fold.png`). The route into the map is 13px outline pills (`:274-325`) that read as filters.
- `ledger-coords` (medium): rows end in raw `lat, lng` (`:403`); the dashed blank row 通報第一筆 (`:365-380`) never says first of what and sits against the owner's rule about "nobody has reported yet". No table holds place names.
- `gbif-copy` (medium): `whatBody`, `taironBody` (`:331,333`) claim GBIF publication; `openBody`, same page, says planned. W0c fixes the strings; W4 stops rendering them.
- `tiny-text` (medium): 9px Latin line (`:200`), 11px tracked Hanzi kickers (`:278,343,429`), 0.12em tracking on the h1 (`:193`). On `/en` that line is the only readable name.
- `getStats()` (`:39-48`) runs on `sql`, not `asPublic`; `stats.ts:23 overview()` is the same query as `web_anon`. `Promise.all` (`:123`) lets one failed query 500 the page.
- Below the fold, seven sections of equal volume, ~720 Chinese characters.

**Scope**
In: `app/[locale]/page.tsx` (rebuild); the `home.*` catalogue; `anniversaryLedger` in `lib/stats.ts`; `test/home.test.mjs`; `checkHome` (`e2e/pages.spec.mjs:80-167`); `scripts/verify-deploy.mjs:66-72`; the interim emblem upscale.
Out: chrome, `SiteChrome` (W3); tokens, fonts, primitives (W2); `?category=` on arrival, `deriveCategory`, injured wording (W6); `/map` deep-link parsing (W5); a `taiwan_admin` place-name table (W8, whose list and detail print the same coordinates: `reports/(list)/page.tsx:289`, `reports/[id]/page.tsx:209`); rehoming the explanation to /about (W10); English common names (W7); baselines (W13); the badge redraw (the team).

**Depends on / Blocks**
W1's decision (skin, plus "one report action, no map picture"); W2 (`Section`, `Button`, `DataRow`, `LinkAction`, `Binomial`, the preloaded `sign-home` face, `design-guard`); W3 (`SiteChrome variant="home"`, which also removes the `cookies()` read that makes home dynamic today). Rebase on W0c, same keys. Independent of W6: a plain `/report` link works against today's form. Blocks only W13's home baselines.

**Decisions needed from the owner** (none blocks starting)
1. Emblem at 520/300px now, still lettered PROJECT ECOWATCH, or wait for the redraw. Default: now — it is already live at 320px.
2. Does the ledger survive? Default: yes, reduced — one row per year, no blank row, no coordinates. If no: delete block 2, `anniversaryLedger`, its test.
3. Place names instead of coordinates. Default: not here; if W8 lands `taiwan_admin`, a one-commit follow-up joins it on `location_public`.
4. Sign label: live `home.ctaReport` 通報一筆 / File a report, or the flow chapter's 通報動物 / Report an animal. Default: live.
5. `/en` h1 reads "FormosaWatch", Chinese secondary; the hero credit links to `/attribution`, so the outbound roadkill.tw link leaves this page. Default: yes to both.
6. If W3's tab bar is refused, the phone hero gains a second outlined sign 看地圖.

**Design spec**
Structure is identical in both directions; only the skin differs. Roundel puts the hero on `plate` and block 1 on the page's one `field` Section; Field journal keeps both on paper, opening each block with a 2px rule and margin heading.

Hero — a `Section` at `min-height: min(640px, 100svh - chrome)`; names hard-coded, no query.
- ≥1280px: emblem 520px left (400px when the viewport is ≤760px tall), 40px gap, h1 `text-hero` on two lines — keep the two `whitespace-nowrap` halves so a break can only fall between 福爾摩沙 and 守望計畫 — `home.tagline` at `text-lead`, two 56px signs: primary `/report`, secondary `/map`.
- 1024-1279: emblem 360px, h1 `text-display` ("FormosaWatch" at 80px is ~575px and overflows).
- <1024: stacked, centred, 16px gutters (eight Hanzi at 40px need 320px), emblem `min(300px, 77vw)` (360px from 640px up), h1 `text-title`, tagline `text-body`, one full-width primary sign; 看地圖 is the tab bar's first item.
- Hero foot, `text-note`: one line from the live `provenanceSource` / `provenanceOrg` / `provenanceVia`, linking to `/attribution`.
- `/en`: h1 "FormosaWatch", then `<span lang="zh-TW">` with the Chinese name at `text-head`. On zh there is no Latin second line, no dot, hairline, shadow or kicker, and one ember element per viewport (W3 outlines the chrome's 通報 here).

Below:
1. Heading `home.mapKicker`, no picture. Two columns (one on a phone) of 64px `DataRow`s, names at `text-head`: four places under `mapPlacesLabel` (`/map?lng=&lat=&z=11`), three `mapEntrySpecies` under `mapSpeciesLabel` (`/map?taxonId=`); then an outlined sign `home.ctaMap`. On `/en` a species row is the `Binomial` with the zh name beneath, until W7.
2. `home.ledgerTitle`: one row per year, newest first, at most seven, each linking to `/reports/[id]` — long-form date in `tabular-nums`, name, binomial. LinkActions `ledgerAll`, `speciesLink`.
3. One `text-head` credit sentence with a LinkAction to `/about`.

States: the three queries run under `Promise.allSettled`; a block whose query rejects or returns nothing is not rendered, and nothing is written about the absence. No motion.

Copy. Delete `ask`, `askHint`, `door*`, `firstTime`, `what*`, `how*`, `trust*`, `open*`, `close*`, `mapLine`, `ledgerHint`, `ledgerBlank`, `provenanceCounts`. Leave `who*` and `tairon*` unrendered in the catalogue: /about has no equivalent yet and W10 moves them.

| Key | zh-TW | en | Status |
|---|---|---|---|
| `ctaMapShort` | 看地圖 | Open the map | needs native read; fallback `nav.map` |
| `credit` | 這些紀錄，是路殺社志工在路邊一筆一筆記下來的。 | These records were made at the roadside, one at a time, by 路殺社 (TaiRON) volunteers. | needs native read; fallback: omit block 3 |

**Implementation steps**
1. `test/home.test.mjs:17-23`: read the four place links from the rendered HTML, not the hand-kept `MAP_PLACES` mirror.
2. `page.tsx`: `overview()` replaces `getStats()`; `Promise.all` becomes `allSettled`; pass `String(obscured)` to the footer.
3. `stats.ts anniversaryLedger`: `partition by extract(year from r.observed_at)` instead of `observed_at::date`; stop selecting `lat`/`lng`. The date partition plus `order by "observedAt" desc` clusters rows on the newest year — `limit 5` returns 2017 only, contradicting the title. Keep the `MM-DD` window and `not r.is_obscured`.
4. Absent the redraw: commit a one-time 1040px Lanczos upscale over `public/brand-badge.png` (next/image never enlarges a 512px source).
5. `page.tsx`: new hero; delete `DOORS`, the provenance bar, the `CATEGORIES` / `REPORT_GROUPS` / `Category` imports and the `report` translator. `Badge priority sizes="(min-width:1280px) 520px, (min-width:640px) 360px, 77vw"`; import W2's preloading `sign-home`.
6. `page.tsx`: blocks 1-3; delete the six old sections; rewrite the docblock — keep its reasoning on the credit, the absent data picture and the absent photographs, and add why the doors went.
7. `messages/*.json`: keys change in the same commit as the JSX that stops using them (CI checks parity).
8. Rewrite `checkHome` and `home.test.mjs` as intent, not geometry. `verify-deploy.mjs:66-72` greps served HTML for `project formosawatch`: the hero drops that line and W3 drops the wordmark from home's header, so once both land `/` no longer contains it. Replace it with "exactly one `/report` link, no `?category=`".

**Acceptance criteria**
- `/` and `/en`: zero `a[href*="/report?category="]`; the hero holds exactly one `/report` link and, from 1024px, one `/map` link.
- Emblem 520px at 1440×900, 400px at 1366×700, 360px at 1024×768, ≥70% of width at 390×844 and 360×740. At all five, both locales: the report sign ends inside the viewport less the tab bar, neither half of the name breaks internally, and the credit line also clears at 1440×900 and 390×844. No sideways scroll at 320, 768, 1024, 1280 on `/en`, where `h1 [lang="zh-TW"]` still exists.
- Kept: three species links, non-sensitive, unprotected, zh-named (`home.test.mjs:62-80`); every place link opens a view with records; `/field.svg` still 404s; one badge preload. Ledger 0-7 rows, all distinct years (`supabase/seed-test.sql` is sparse — assert distinctness, not a count).
- Home HTML matches no `\d{2}\.\d{4}, \d{3}\.\d{4}`; `page.tsx` does not import `sql`.
- Zero `page.tsx` entries in W2's `design-debt.json`; no Hanzi under 14px; axe clean; `_contrast.mjs` silent for `/` and `/en`; links ≥44px; focus visible on each surface.
- No `/maplibre/` or tile request; no new client component; HTML no larger than today (99 KB raw, 22 KB gzip); font preload inside W2's 60 KB budget; CLS < 0.05; Lighthouse mobile LCP ≤ 2.5 s. W4 adds no dynamic API, so `/[locale]` is static once W3's `cookies()` read is gone.
- Sign-off shots: fold and full at 1440×900 and 390×844 @2x for `/` and `/en`; fold at 1366×700 and 360×740.

**How to verify**
Repo root, `npm run dev` up: `node --test apps/web/test/home.test.mjs apps/web/test/i18n.test.mjs apps/web/test/design-guard.test.mjs`; `npm run test:pages`; `node apps/web/e2e/_contrast.mjs`, then `--mobile`; `SHOTS_DIR=<dir> node apps/web/e2e/_shots.mjs home`, then `--mobile` (add `["/en","home-en",2500]`). Node's `fetch` and default Playwright send no Accept-Language, so `/` renders zh-TW; request `/en` explicitly for every English assertion. A `JSON.parse` 500 on an unrelated page after a message edit is a corrupt dev cache: stop the server, `rm -rf apps/web/.next`, restart — never delete it while the server runs.

**Risks and traps**
- `page.tsx:167-169`: "stacked on a phone, the name fell below the fold under a 300px circle." Stacking fits now only because the doors are gone; the fold assertion is the guard.
- `Badge.tsx:12`: "`rounded-full` is doing real work. The source artwork is a circle on a white field" — the one exemption to W2's ban.
- `stats.ts`: "Obscured records are excluded, and not only for tidiness" — a misidentified 狼 would otherwise reach the front page. Never swap the ledger for latest records: every row would read 2017-12-31. Its date renders through `toISOString()` while the query windows in Asia/Taipei; change both or neither.
- `home.test.mjs:63-65`: "a test that copied the query would keep passing if the sensitivity filter were dropped from both." Keep asserting on rendered links.
- `REPORT_GROUPS` and `report.group.*` stay: the form and the map filters read them.
- Shared files: `messages/*.json` (only `home.*`); `pages.spec.mjs` (only `checkHome`); `page.tsx` (branch after W3's `SiteChrome` commit); `public/brand-badge.png` (W3's header reads it at `sizes="48px"`).

**Suggested PR breakdown and effort**
PR 1 (steps 1-3): plumbing and ledger query, no visual change; may precede W2. 0.5 day.
PR 2 (steps 4, 5, 7, 8): hero; doors and provenance bar gone; both test files rewritten. 1.5 days.
PR 3 (steps 6-8): blocks 1-3, six old sections deleted. 1 day.
Total M: ~3 person-days (6-8 agent-hours plus review). Unread zh-TW strings stay out of production behind the fallbacks above.
